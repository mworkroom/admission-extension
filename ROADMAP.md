# Admission Extension Roadmap

## 이 문서의 역할

이 문서는 프로젝트가 지금 어디까지 왔는지 확인하기 위한 길잡이다.

- 현재 진행 중인 단계는 하나만 둔다.
- 완료되지 않은 기능을 완료로 표시하지 않는다.
- 새로운 아이디어는 현재 단계에 바로 넣지 않고 `후속 검토`에 기록한다.
- 실제 업무에서 확인된 실패를 기준으로 다음 단계를 조정한다.

## 상태 표시

| 상태 | 의미 |
| --- | --- |
| `완료` | 완료 조건을 충족했고 검증도 끝난 상태 |
| `진행 중` | 현재 구현하거나 검증하는 단계 |
| `다음 작업` | 현재 단계가 끝난 뒤 시작할 단계 |
| `대기` | 순서가 오지 않았거나 선행 작업이 필요한 단계 |
| `실사용 후 결정` | 프로토타입 사용 기록을 본 뒤 범위를 정할 항목 |

## 현재 위치

| 단계 | 상태 | 핵심 결과 |
| --- | --- | --- |
| Phase 0 — 확장 프로그램 기본 골격 | `완료` | Side Panel과 고정된 11개 항목 화면 |
| Phase 1 — KCL 한 과정 완주 | `완료` | 현재 페이지와 Fees 페이지의 정보를 한 화면에 표시 |
| Phase 2 — SOAS·QMUL 일반화 검증 | `완료` | 긴 단일 페이지, 여러 입학 연도, South Korea 안전 자동 확인 |
| Phase 3 — 대학·단과대 공통 메모 | `완료` | 반복 확인 정보를 학년도별로 검증해 재사용 |
| Phase 4 — 실사용 피드백과 반자동 작업 보조 | `완료` | 핵심 정보·반복 예외·앱 문제를 낮은 인지부하로 분리 |
| Phase 5 — 반복 실패 개선 | `실사용 후 결정` | 학교별 예외 규칙과 추가 자동화 |

현재 상태:

- 제품 방향과 첫 테스트 학교 3곳을 정했다.
- Phase 0 구현과 자동 검사를 완료했다.
- 실제 Chrome에서 압축 해제 설치, 확장 아이콘 클릭, Side Panel 연결, 현재 페이지 정보, 기준 저장 유지를 확인했다.
- 제한 페이지와 저장 실패 안내는 로컬 예외 검증으로 확인했다.
- Phase 1 계획서를 작성했다.
- Phase 1의 실제 KCL DOM 확인, parser, Side Panel 결과 UI, 로컬 저장과 자동 검사 구현을 완료했다.
- Phase 2 계획서를 작성했다.
- SOAS·QMUL 실제 DOM 확인, 공통 snapshot schema 3, 학교 판정 registry, 두 학교 adapter와 공통 parser를 구현했다.
- KCL custom dropdown의 South Korea 안전 자동 선택과 exact alias 제한을 추가했다.
- `0.3.0` 실사용에서 발견된 KCL 과정명 누락과 QMUL 등급 불일치를 수정해 `0.3.1`로 올렸다.
- 실제 Chrome에서 `0.3.1`의 KCL, QMUL, SOAS 동작이 모두 정상임을 확인했다.
- Phase 1과 Phase 2를 완료로 전환했다.
- Phase 3 대학·단과대 공통 메모 계획서를 작성했다.
- P3-0 메모 schema, 학년도 확인 상태, 범위 resolver와 값 충돌 비교를 구현했다.
- P3-1 `commonMemoStore` 저장, 손상 record 격리와 상위 schema 보호를 구현했다.
- P3-2 공통 메모 조회, 학년도 확인 상태, 출처·복사와 페이지 값 비교를 구현했다.
- J님의 사용 흐름에 맞춰 별도 `03 공통 메모` 섹션을 제거하고, English Requirements와 University Application Deadline 카드 안에 `확인한 사항`으로 배치해 버전을 `0.4.1`로 올렸다.
- 별도 안내 링크가 있는 영어 조건은 카드에서 URL을 바로 열 수 있게 했다.
- 430px 로컬 UI 검증에서 인라인 메모 연결, 같음·다름·페이지 구체값 없음 표시와 가로 넘침 없음을 확인했다.
- P3-3에서 대상 카드의 `확인한 사항 추가`, 기존 내용 수정, URL 검증과 로컬 저장을 구현해 버전을 `0.5.0`으로 올렸다.
- 같은 대학·항목 id는 새 record를 만들지 않고 기존 record를 수정하며 학년도별 확인 기록을 유지한다.
- School·Faculty는 정확한 조직 식별자가 없는 상태에서 잘못 적용하지 않도록 입력 UI를 열지 않고 University 범위만 제공한다.
- Application Fee, Reference, SOP Guideline에도 카드 내부 `확인한 사항 추가`를 연결해 메모 대상 다섯 항목을 완성하고 버전을 `0.5.1`로 올렸다.
- SOAS Reference의 앱 작성 요약문을 제거하고, 화면·복사·출처에서 페이지 원문 문장을 그대로 사용하도록 수정했다.
- P3-4에서 `이번 학년도 확인 완료` 저장, 새 학년도 `재확인 필요`, 확인 후 값 변경 상태를 연결해 버전을 `0.6.0`으로 올렸다.
- 확인 기록은 학년도별로 보존하며, 이전 학년도로 돌아가면 기존 확인 상태를 복원한다.
- 430px 로컬 Side Panel harness에서 확인 전·확인 완료·재확인 필요·확인 후 변경 흐름과 가로 넘침 없음을 검증했다.
- 실제 Chrome 확장 재로딩과 실사이트 검증은 아직 진행하지 않았다.
- P3-5에서 대학·항목·적용 범위·현재 값과 학년도별 확인 기록을 보여주는 삭제 확인 dialog를 구현해 버전을 `0.7.0`으로 올렸다.
- 취소 시 기존 record를 유지하고, 저장 성공 시 선택한 record만 제거하며, 저장 실패 시 dialog와 기존 메모를 유지한다.
- 430px 로컬 Side Panel harness에서 삭제 대상 표시, 취소, 삭제 후 다른 메모 보존, 강제 저장 실패와 가로 넘침 없음을 검증했다.
- 실제 Chrome 확장 재로딩과 실사이트 삭제 검증은 아직 진행하지 않았다.
- P3-6 전체 자동 검사와 KCL·SOAS·QMUL 430px UI 회귀를 통과했다.
- native dialog의 접근성 이름, 초기 포커스와 취소 경로를 자동 검사로 고정했다.
- 실제 Chrome에서 세 학교 과정 탭 연결과 KCL 원문 DOM은 확인했지만, Side Panel은 자동화에서 직접 제어할 수 없어 수동 검증으로 분리했다.
- J님의 실제 Chrome 수동 검증에서 메모 재사용·저장 유지·삭제·키보드와 세 학교 기본 회귀는 통과했다.
- 새 학년도 전환 후 재확인 UI가 갱신되지 않는 실사용 문제를 수정하고, 확정 메모의 중복 ‘페이지 값 없음’ 안내 제거와 상단 기준 패널 sticky 해제를 반영해 버전을 `0.7.1`로 올렸다.
- 자동 검사 60개와 KCL·SOAS·QMUL 430px 회귀를 통과했다.
- J님이 실제 Chrome `0.7.1`에서 학년도 전환 후 재확인 UI, 확정 Application Fee의 중복 안내 제거, 상단 기준 패널의 일반 스크롤을 모두 확인해 Phase 3을 완료했다.
- Phase 4 계획서를 작성하고 P4-0으로 알려진 상담 위젯 숨김 기능을 구현해 버전을 `0.8.0`으로 올렸다.
- Unibuddy의 확인된 id·iframe 주소·title만 대상으로 하며, 현재 탭에서 숨김 규칙을 즉시 적용·해제할 수 있는 기본 활성 설정을 추가했다.
- 새 권한 없이 자동 검사 69개를 통과했고 KCL·SOAS·QMUL 430px 로컬 UI와 Unibuddy 모의 위젯 숨김·복원을 확인했다.
- J님이 실제 Chrome `0.8.0`의 QMUL 과정 페이지에서 Unibuddy 위젯이 정상적으로 숨겨지는 것을 확인해 P4-0을 완료했다.
- P4-1에서 `activeCourseWork` schema 1, 과정·입학 기준 식별자, 공식 출처 페이지 목록, 분석값·수동값 origin 구분과 손상 데이터 격리를 구현했다.
- 자동 검사 78개를 통과했으며 P4-1은 UI를 변경하지 않아 확장 버전 `0.8.0`을 유지했다.
- P4-2에서 현재 분석으로 활성 작업을 시작하거나 같은 작업에 추가하는 UI를 연결해 버전을 `0.9.0`으로 올렸다.
- 같은 값은 entry를 중복하지 않고 공식 출처만 합치며, 다른 실제 값은 모두 보존하고 충돌 항목으로 집계한다.
- 다른 과정 또는 입학 기준은 기존 작업에 병합하지 않고, 현재 작업과 새 작업을 비교하는 확인 dialog에서 명시적으로 교체한 경우에만 새 작업을 시작한다.
- 자동 검사 86개와 KCL·SOAS·QMUL 430px 로컬 UI 회귀를 통과했다.
- P4-3에서 분석값을 보존한 직접 입력·수정, 현재 페이지·확인한 사항·활성 작업 값의 항목별 복사 기록을 연결해 버전을 `0.10.0`으로 올렸다.
- 추출 결과마다 status와 reason code를 기록하고, 후속 확인 원인을 `site_structure`, `separate_page`, `application_check`로 구분해 카드에도 표시한다.
- 직접 입력값과 생성·수정 기록은 같은 storage 쓰기로 저장하며, 손상 기록이나 저장 실패 시 기존 활성 작업과 실사용 기록을 덮어쓰지 않는다.
- 자동 검사 97개와 KCL·SOAS·QMUL 430px 로컬 UI 회귀, 직접 입력·수정·복사와 저장 실패 보호를 통과했다.
- P4-4에서 활성 작업 JSON과 전체 실사용 기록 CSV 내보내기, 학교·항목별 실패 요약을 구현해 버전을 `0.11.0`으로 올렸다.
- 반복 실패 중 `site_structure`가 3회 이상이면서 서로 다른 과정이 2개 이상인 항목만 다음 adapter 개선 후보로 표시한다.
- Blob download를 사용해 새 권한 없이 내보내고, CSV에는 UTF-8 BOM·CRLF와 14개 고정 열을 적용했다.
- P4-5에서 자동 검사 104개와 저장 실패 보호를 다시 통과하고, KCL·SOAS·QMUL 430px 로컬 UI에서 11개 카드, 과정 교체와 가로 넘침 없음을 확인했다.
- 실제 Chrome에서 발견된 KCL SOP의 잘못된 `지원서 확인 필요` 분류를 수정해 버전을 `0.11.1`로 올리고 자동 검사 105개와 430px KCL UI 회귀를 통과했다.
- J님이 실제 Chrome에서 `0.11.1`의 KCL SOP 실패 분류가 사라진 것을 확인했다.
- 실사용에서 발견된 Alliance Manchester Business School의 과정 URL 미지원 문제를 해결해 Manchester MSc 과정 탭 adapter를 추가하고 버전을 `0.12.0`으로 올렸다.
- Manchester adapter는 과정별 Overview·Entry requirements·Application and selection과 학교 공통 Entry requirements·How to apply·Supporting documents의 공식 원문을 합쳐 11개 항목과 각 출처를 만든다.
- 자동 검사 106개와 Manchester 430px 로컬 UI 회귀를 통과했고, 실제 Chrome에서 관련 공식 페이지의 DOM과 추출 패턴을 확인했다. 실제 `0.12.0` Side Panel 분석은 J님의 수동 확인이 남아 있다.
- 학교를 하나씩 허용 목록에 추가하는 구조가 실사용 데이터 수집 목적과 맞지 않는다는 피드백을 반영해 `0.13.0`에서 학교 whitelist를 제거했다.
- 모든 일반 HTTPS 공개 대학 페이지는 도메인별 generic reader로 분석하며, 정밀 adapter가 있는 네 학교만 기존 판독기를 우선 사용한다.
- generic reader는 현재 페이지의 학교·과정·입학요건·한국 조건·영어 안내·국제 학비·지원비·마감일·Reference·SOP·CV를 보수적으로 찾고, 찾지 못한 항목도 실패 기록으로 남긴다.
- 실제 Bristol MSc Marketing 공개 페이지에서 generic 판독을 확인하고, 자동 검사 107개와 430px generic UI 회귀를 통과했다. 실제 확장 `0.13.0` Side Panel 확인은 남아 있다.
- `0.14.0`에서 앱이 미리 분류하지 못한 예외를 화면 맨 아래 `문제 기록`에 자유롭게 남기는 기능을 추가했다.
- 문제 기록은 활성 작업 없이도 현재 학교·과정·URL·학년도·시각을 자동 연결하며, 생성·수정·삭제 이력을 기존 실사용 CSV에 포함한다.
- 자동 검사 119개와 430×900 로컬 UI에서 문제 저장·수정·삭제·CSV 활성화·저장 실패 보호 및 가로 넘침 없음을 확인했다. 실제 Chrome 확장 `0.14.0` 검증은 남아 있다.
- `0.15.0`에서 실제 업무의 기준 문서는 같은 대학의 최근 Word 파일이라는 점을 반영해 앱의 목적을 반자동 확인 보조로 다시 정리했다.
- 여러 공식 페이지 값을 앱 안에서 합치던 활성 작업·직접 입력·실사용 CSV 기능은 UI와 실행 코드에서 제거했다. 기존 `activeCourseWork`와 `activeCourseWorkEvents` 로컬 저장값을 삭제하는 코드는 넣지 않았다.
- 기본 화면은 Entry Requirements·Tuition Fee·Application Fee·IELTS와 메모·추출 실패·추가 확인 항목만 먼저 표시하고, 나머지 정상 항목은 접어둔다.
- 복사 버튼은 Entry Requirements·Tuition Fee·Application Fee에만 남겼으며 IELTS와 확인한 사항에는 제공하지 않는다.
- 확인한 사항은 대학 전체 또는 항목별 한 줄 요약, 선택 상세·출처, 명시적인 확인 상태·확인일을 사용한다. 현재 학년도 확인이 없으면 `다시 확인`, 근거가 확정되지 않았으면 `미확인`으로 표시한다.
- 문제 기록은 활성 작업 로그에서 분리하고 도구 dialog에서 미해결·해결됨 상태와 전체 JSON 내보내기를 지원한다.
- 자동 검사 68개와 430×850·360×780 로컬 UI 검증을 통과했다. 실제 Chrome에 `0.15.0`을 다시 로드한 실사이트 확인은 남아 있다.

---

## Phase 0 — 확장 프로그램 기본 골격

**상태: `완료`**

### 목표

Chrome 확장 아이콘을 누르면 현재 탭에 연결된 Side Panel이 열리고, 이후 추출 기능을 넣을 수 있는 고정 화면과 로컬 상태 구조를 만든다.

### 주요 작업

- Manifest V3 확장 프로그램 골격
- 확장 아이콘 클릭 시 Side Panel 열기
- 현재 페이지 제목과 URL 표시
- 앱 전체 기준 학년도·입학 회차 표시 및 변경
- 정해진 11개 항목 카드 표시
- 색상 외에도 아이콘과 문구로 상태 표시
- 브라우저 로컬 저장소의 기본 구조
- 최소 자동 검사와 Chrome 수동 설치 확인

### 완료 조건

- [x] 압축 해제된 확장 프로그램을 Chrome에 설치할 수 있다.
- [x] 확장 아이콘을 누르면 오른쪽 Side Panel이 열린다.
- [x] Side Panel에 현재 페이지 제목과 URL이 표시된다.
- [x] 상단에 현재 앱 기준이 계속 표시된다.
- [x] 기준을 변경하면 다음에 열어도 유지된다.
- [x] 11개 항목 카드가 정해진 순서로 표시된다.
- [x] 카드 상태가 `분석 전`으로 명확하게 보인다.
- [x] 주요 컨트롤이 네이티브 버튼·입력·선택 요소와 명확한 포커스 표시로 구성된다.
- [x] 자동 검사와 Chrome 수동 검증을 통과한다.

상세 계획: [Phase 0 계획서](docs/spec/PHASE_0_PLAN.md)

---

## Phase 1 — KCL 한 과정 완주

**상태: `완료`**

### 목표

KCL Nutrition Requirements 페이지 하나를 열었을 때 현재 페이지의 정보와 Fees 페이지의 학비를 같은 Side Panel에서 확인하고 항목별로 복사한다.

### 주요 작업

- University와 Course 인식
- Entry Requirements 추출
- Application Fee 추출
- SOP, Reference, CV 추출
- English Band 표시
- 현재 DOM에서 `Fees` 링크 발견
- 현재 화면을 떠나지 않고 Fees 페이지 확인
- 국제학생 Full-time 학비와 학년도 추출
- 항목별 일반 텍스트 복사
- 출처 URL과 원문 위치 표시
- 추출·실패·복사 기록 저장

### 완료 조건

- [x] KCL Requirements 페이지의 정해진 정보를 올바른 카드에 표시한다.
- [x] URL을 추측하지 않고 실제 `Fees` 링크를 발견한다.
- [x] Fees 페이지를 뒤에서 확인해 국제학생 학비를 표시한다.
- [x] 가져오지 못한 항목은 이유와 다음 행동을 표시한다.
- [x] 복사한 항목은 `복사 완료`로 바뀐다.
- [x] 모든 결과에 출처가 남는다.
- [x] 학년도·입학 회차가 현재 앱 기준과 다르면 경고한다.

상세 계획: [Phase 1 계획서](docs/spec/PHASE_1_PLAN.md)

---

## Phase 2 — SOAS·QMUL 일반화 검증

**상태: `완료`**

### 목표

KCL에만 맞춘 규칙이 되지 않도록 서로 다른 페이지 구조에서 공통 추출 규칙을 검증한다.

### SOAS 검증

- 긴 단일 페이지에서 필요한 구간만 수집
- International tuition fee 추출
- South Korea 학력 기준 분리
- 학년도 표기가 없는 값의 상태 처리
- 공통 IELTS 페이지로 가야 하는 상황 표시

### QMUL 검증

- September 2026과 September 2027 후보 분리
- 현재 앱 기준에 맞는 후보를 먼저 표시
- 다른 연도 후보를 숨기지 않기
- `Fees will be announced`를 이전 연도 학비로 대체하지 않기
- `Deadline to be confirmed`를 정확한 미정 상태로 표시
- 단과대 English Band 인식

### 공통 South Korea 처리

- 이미 렌더링된 South Korea 블록을 먼저 사용
- 정확한 국가명 별칭만 허용
- 필요한 경우 native select 또는 custom dropdown 자동 선택
- 선택 후 실제 학력 안내가 나타났는지 재검증
- North Korea 또는 모호한 후보 자동 선택 금지

### 완료 조건

- [x] 학교별 DOM adapter와 공통 후보 선택 규칙을 분리한다.
- [x] 세 학교에서 같은 항목 데이터 형식과 상태 체계를 사용한다.
- [x] reason code로 미발표·기준 없음·본문 없음 상태를 기록한다.
- [x] 확장 버전 `0.3.1`을 실제 Chrome Side Panel에서 최종 확인한다.
- [x] 실사용 검증으로 AI 없이 가능한 범위를 확정한다.

상세 계획: [Phase 2 계획서](docs/spec/PHASE_2_PLAN.md)

---

## Phase 3 — 대학·단과대 공통 메모

**상태: `완료`**

### 목표

IELTS, Deadline 안내처럼 매 과정마다 다시 찾을 필요가 없는 정보를 확장 프로그램에서 바로 확인한다.

### 주요 작업

- 대학 전체 메모와 School·Faculty 메모 구분
- 항목별 값, 출처 URL, 마지막 확인일 저장
- 적용 학년도 저장
- `이번 학년도 확인 완료` 수동 확정
- 항목별 확인 상태
- 새 학년도 전환 시 값은 유지하고 확인 상태만 초기화
- 현재 페이지 값과 메모가 다르면 둘 다 표시
- 페이지의 더 구체적인 값을 위에 표시하고 자동 덮어쓰지 않기

### 완료 조건

- [x] 같은 대학의 다른 과정에서도 공통 메모를 다시 사용할 수 있다.
- [x] 정확한 조직 식별자가 있을 때만 School·Faculty 범위를 구분하고, 식별자가 없으면 대학 메모만 안전하게 연결한다.
- [x] 어느 학년도에 확인한 값인지 바로 알 수 있다.
- [x] 지난 학년도 값과 현재 학년도 확인 상태를 혼동하지 않는다.
- [x] 자동 검사와 실제 Chrome 수동 검증을 통과한다.

상세 계획: [Phase 3 계획서](docs/spec/PHASE_3_PLAN.md)

---

## Phase 4 — 실사용 피드백과 반자동 작업 보조

**상태: `완료`**

### 목표

공개 페이지에서 보수적으로 찾은 핵심 정보와 다음 업무에서 다시 볼 예외, 앱 개선용 문제 기록을 서로 섞지 않고 표시한다.

### 주요 작업

- 확인된 공급자 서명 기반 상담 위젯 숨김
- 패널 열기와 탭 URL 변경 시 보수적 자동 분석
- 과정별 핵심값과 메모·문제 항목 우선 표시
- 대학 전체·항목별 확인한 사항과 학년도별 확인일
- 문제 기록의 미해결·해결됨 관리와 JSON 내보내기
- 기존 활성 작업 저장값을 삭제하지 않는 비파괴적 기능 제거

### 완료 조건

- [x] 새 권한 없이 Unibuddy 위젯을 현재 탭에서 숨기고 설정을 끄면 즉시 복원한다.
- [x] 핵심 복사 대상과 확인용 표시 항목을 구분한다.
- [x] 반복 예외를 한 줄 메모로 바로 확인하고 현재 학년도 확인 여부를 구분한다.
- [x] 미리 정의하지 못한 앱 문제를 현재 페이지 문맥과 함께 기록한다.
- [x] 문제 기록을 해결 상태와 함께 JSON으로 내보낼 수 있다.
- [x] 활성 작업 없이도 분석·메모·문제 기록이 독립적으로 동작한다.
- [x] 다음 개선 대상을 기록에서 결정할 수 있다.

상세 계획: [Phase 4 계획서](docs/spec/PHASE_4_PLAN.md)

---

## Phase 5 — 반복 실패 개선

**상태: `실사용 후 결정`**

### 목표

실사용 기록에서 반복적으로 확인된 문제만 해결한다.

### 후보 작업

- 학교별 Fee Lookup 규칙
- 자주 쓰는 학교의 관련 링크 바로가기
- 학교별 Deadline·IELTS 예외
- JavaScript로 늦게 표시되는 정보 처리
- AI 분류가 실제로 필요한지 재검토

### 시작 조건

- Phase 4 기록에서 같은 실패가 반복된다.
- 일반 규칙으로 해결하기 어렵다는 근거가 있다.
- 자동화했을 때 실제 클릭과 판단이 줄어든다.

---

## 현재 제외 범위

다음 기능은 현재 Roadmap에 포함하지 않는다.

- Word 문서 생성 또는 자동 입력
- admission-guidelines-automation 앱과의 연결
- Notion 자동 읽기·저장
- 로그인된 지원서 페이지 분석
- CAPTCHA 처리
- 전체 대학 사이트 자동 탐색
- 전체 결과 한 번에 복사
- AI가 원문에 없는 내용을 작성하거나 추측하는 기능

## 참고 페이지

- [KCL Nutrition MSc — Requirements](https://www.kcl.ac.uk/study/postgraduate-taught/courses/nutrition-msc/requirements)
- [KCL Nutrition MSc — Fees](https://www.kcl.ac.uk/study/postgraduate-taught/courses/nutrition-msc/fees)
- [SOAS MSc Global Development](https://www.soas.ac.uk/study/find-course/msc-global-development)
- [QMUL Corporate Finance MSc](https://www.qmul.ac.uk/postgraduate/taught/coursefinder/courses/corporate-finance-msc/)
- [Alliance MBS MSc Marketing — Entry requirements](https://www.alliancembs.manchester.ac.uk/study/masters/msc-marketing/entry-requirements/)
