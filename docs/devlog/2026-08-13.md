# 2026-08-13 Admission Extension Development Log

## Work Summary

CSV에 정리된 13개 학교의 실제 금액 문맥 조사 결과를 바탕으로, 페이지에서 가장 큰 금액을 학비로 선택하던 식의 크기 추정을 사용하지 않고 금액의 의미와 적용 기준을 먼저 판정하는 범용 학비 수집기를 구현했다.

## Work Completed

- 페이지의 GBP 금액을 학비, 지원비, 보증금, 장학금·할인, 기타 비용으로 분류하는 공통 후보 수집 단계를 추가했다.
- 학비 후보에 가까운 학년도, 입학 시기, Full-time·Part-time, Home·International 문맥을 연결했다.
- 금액 주변에 학년도가 없을 때는 페이지 전체에서 학년도가 하나만 확인된 경우에만 그 학년도를 상속하도록 했다.
- 여러 학년도가 함께 표시된 페이지에서는 학년도를 임의로 상속하지 않고 사용자 확인 상태를 유지하도록 했다.
- 연결된 일반 학비 페이지에서도 보증금, 장학금, 생활비 등의 금액을 학비 후보에서 제외하고 현재 학년도 후보만 남기도록 보강했다.
- 현재 기준과 정확히 일치하는 학비 후보가 학년도 없는 후보보다 우선되도록 parser의 후보 구체성 비교를 추가했다.
- Manchester, UCL, Edinburgh, Leeds, Warwick, Birmingham, Nottingham, Bristol, York, Loughborough, Sheffield, Southampton, Exeter의 금액 문맥을 재현한 회귀 테스트를 추가했다.
- 단일 학년도 상속과 복수 학년도 비확정 동작을 별도 테스트로 고정했다.

## Decisions

- 금액의 크기는 학비 판정 조건으로 사용하지 않는다. 전체 장학 재원, 시설 투자비, 생활비, 보증금, 졸업생 연봉이 학비보다 크거나 작을 수 있기 때문이다.
- 금액은 먼저 주변 문맥으로 유형을 분류하고, 학비로 분류된 후보에만 학년도·입학 시기·수학 형태·fee status 조건을 적용한다.
- 페이지의 학년도는 사용자 설정값으로 덮어쓰지 않는다. 실제 페이지에서 단일 학년도가 확인된 경우에만 보조 근거로 사용한다.
- Edinburgh와 Warwick처럼 공식 연결 학비표가 있는 구조는 기존 링크 추적과 과정 코드 필터를 유지하고 공통 금액 분류 안전장치만 추가한다.

## Current Status

- 13개 학교 금액 유형 회귀군과 기존 generic reader·course parser 집중 테스트가 통과했다.
- `npm.cmd run check`로 전체 문법 검사와 92개 테스트가 모두 통과했다.
- 학비·지원비·보증금·장학금·기타 분류 결과는 snapshot의 `moneyCandidates`에 보존되며, UI의 Tuition Fee에는 현재 기준과 일치하는 학비만 전달된다.
- 실제 설치된 Chrome 확장 프로그램에서의 최종 확인은 최신 로컬 확장을 다시 로드한 뒤 진행해야 한다.
- 변경은 로컬에만 있으며 커밋하거나 배포하지 않았다.

## Next Steps

- Chrome 확장을 다시 로드하고 우선 Southampton, Birmingham, Loughborough에서 기타 금액이 학비로 섞이지 않는지 확인한다.
- 실제 페이지에서 새로운 오분류가 발견되면 학교명 예외가 아니라 금액 문맥 분류 계열을 회귀 사례로 추가한다.

## Work Summary - 추출기 Trial & Error 회고 및 대응 계획 문서화

이번 세션에서 진행한 범용 추출 실험, 학교별 실사용 확인, Entry Requirements 개선, Tuition Fee 실패 분석과 전략 재검토를 하나의 기준 문서로 정리했다. 반복적인 예외 수정에 들어가기 전에 무엇이 실제 원인이었고 앞으로 어떤 조건에서 개발을 계속하거나 중단할지를 명확히 남기는 것이 목적이다.

## Work Completed - 추출기 Trial & Error 회고 및 대응 계획 문서화

- 초기 소수 학교 중심 구현부터 상위 학교 집중 지원, Entry Requirements v2, Tuition Fee 금액 분류 실험, 실제 DOM 재검사까지의 시행착오를 시간 순서로 기록했다.
- table, definition list, card, heading+sibling, hidden tab, dynamic country, linked fee page 등 확인된 DOM 실패 계열과 대표 학교를 정리했다.
- 유지할 UI·저장·원문 포커스 기능과 교체할 generic 추출 계층을 구분했다.
- 구조 보존형 extraction v2, 얇은 학교 profile, 수동 원문 선택, 확인일 저장을 결합한 향후 제품 방향을 정의했다.
- 상위 학교 지원의 ROI, 5단계 구현 계획, 실제 DOM fixture 중심 테스트 원칙, 확대·중단 기준을 문서화했다.

## Decisions - 추출기 Trial & Error 회고 및 대응 계획 문서화

- 앱 전체를 다시 만들거나 15개 학교의 코드를 완전히 복제하지 않고, 추출 계층만 구조 보존형으로 교체한다.
- 미지의 학교에서의 최소 기능은 완전 자동 추출이 아니라 자동 후보와 빠른 수동 선택, 근거·학년도·확인일 저장까지 포함한다.
- 다음 구현 대상은 Tuition Fee이며, 기존 generic 로직에 학교명 예외를 더하기 전에 실제 DOM fixture와 구조 extractor를 먼저 만든다.
- 상위 15개 중 최소 12개가 안정화되지 않으면 범용 자동화 확대를 중단하고 보조 수집 도구로 범위를 고정한다.

## Current Status - 추출기 Trial & Error 회고 및 대응 계획 문서화

- `docs/planning/extraction-trials-and-next-plan.md`에 회고와 향후 대응 계획을 작성했다.
- 이번 작업은 문서만 변경했으며 추출 코드의 동작은 변경하지 않았다.
- 현재 로컬 코드에는 이전 세션의 미커밋 변경이 남아 있으며, 이번 문서 작업에서도 커밋하거나 배포하지 않았다.

## Next Steps - 추출기 Trial & Error 회고 및 대응 계획 문서화

- 문서의 Phase 0에 따라 대표 학교의 실제 DOM fixture를 확보한다.
- Tuition Fee v2에서 table, definition list, card, heading+sibling extractor부터 구현한다.
- 자동 추출과 동시에 사용할 수 있는 수동 원문 선택·확인일 저장의 최소 범위를 확정한다.

## Work Summary - Tuition Fee 구조 보존형 v2

82개 대학의 Tuition Fee DOM 조사에서 확인한 네 가지 공통 구조를 기존 generic reader에 적용했다. 학교별 selector 예외를 늘리지 않고, 금액과 라벨·학년도·fee status의 DOM 관계를 보존하는 공통 후보 계층으로 교체하는 것이 목적이었다.

## Work Completed - Tuition Fee 구조 보존형 v2

- `Table/Grid`, `Key-value/Definition`, `Card/Container`, `Heading/Sibling/Text` 네 구조 extractor를 generic reader에 추가했다.
- 표의 행·열 제목, `dt`·`dd`, 카드 부모와 금액 자식, fee 제목과 다음 형제를 후보 문맥으로 보존했다.
- fee 관련 accordion·tab을 열고 Home·International button/tab/select 중 현재 분석 기준에 맞는 컨트롤을 선택하는 공통 준비 동작을 추가했다.
- 연결된 공식 fee 페이지에도 같은 구조 extractor를 재사용하도록 연결 페이지 처리를 통합했다.
- `TBA`, `not yet set`, `unavailable` 같은 미게시 상태를 금액 누락과 구분해 후보로 보존했다.
- application fee, deposit, scholarship·funding, 생활비·기타 비용 문맥을 tuition보다 우선 제외하도록 분류 순서를 보강했다.
- 실제 DOM 관계를 보존한 대표 fixture를 추가해 York, Bristol, Loughborough, Exeter, Southampton, Birmingham, Manchester, St Andrews와 연결 fee 페이지를 검증했다.

## Decisions - Tuition Fee 구조 보존형 v2

- 82개교를 학교별 reader로 나누지 않고 네 구조 extractor와 공통 page adapter로 처리한다.
- 평면 텍스트 후보는 기존 호환성을 위해 낮은 우선순위 fallback으로 남기고, 구조 후보를 먼저 평가한다.
- 선택하지 않은 audience 값을 분석 기준으로 추정하지 않는다. 실제 audience control을 활성화한 경우에만 현재 fee status를 구조 후보에 보완한다.
- 미게시와 파싱 실패를 분리하며, 학년도·fee status가 모호한 후보는 기존 parser의 확인 필요 상태를 유지한다.
- 수동 원문 선택과 확인 이력 UI는 이번 변경에 섞지 않고 다음 독립 단계로 남긴다.

## Current Status - Tuition Fee 구조 보존형 v2

- `npm.cmd run check`가 통과했으며 총 94개 테스트가 모두 성공했다.
- 기존 Entry Requirements와 KCL·SOAS·QMUL·Manchester 전용 reader, Side Panel UI 및 저장 구조는 변경하지 않았다.
- 코드 fixture와 연결 페이지 회귀는 확인했지만 설치된 Chrome 확장에서의 실제 사이트 검증은 아직 진행하지 않았다.

## Next Steps - Tuition Fee 구조 보존형 v2

- Chrome 확장을 다시 로드하고 York, Bristol, Loughborough, Exeter, Southampton, Birmingham, Edinburgh·Warwick, UCL·Manchester 대표 페이지에서 실제 결과를 확인한다.
- custom select·modal·off-canvas 등 표준 컨트롤로 열리지 않는 실패 사례는 학교명이 아니라 공통 adapter 계열로 추가한다.
- 실제 검증 후 Tuition Fee 수동 원문 선택과 값·URL·학년도·확인일 저장의 최소 UI를 구현한다.
