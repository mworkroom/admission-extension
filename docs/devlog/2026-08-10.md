# 2026-08-10 Admission Extension Development Log

## Work Summary - Sheffield 집중 지원 1차

Sheffield Real Estate MSc 2026 과정 페이지와 연결된 공식 안내 페이지를 실제 브라우저에서 조사했다. 과정 페이지에 있는 항목뿐 아니라 한국 학력 환산표, 무료 지원비, 비자 필요 학생 마감일, 선택 제출 CV처럼 별도 페이지에 분산된 정보도 자동 수집하도록 generic reader를 확장했다.

## Work Completed - Sheffield 집중 지원 1차

- Sheffield 과정 URL을 정식 지원 사이트로 등록하고 대학명과 네 개의 공식 보조 페이지 URL을 설정했다.
- Entry Requirements가 English language와 국제 학력 안내 앞에서 끝나도록 경계를 보강했다.
- South Korea 공식 페이지의 대학원 2:1 표에서 `GPA 3.5/4.5, GPA 3.1/4.3, GPA 3.0/4.0`을 구조화했다.
- 2026/27 Overseas 학비 `£26,320`과 IELTS 6.5를 과정 페이지에서 읽도록 검증했다.
- 공식 지원 안내의 `There is no application fee`를 단순 누락이 아닌 `No application fee` 상태로 처리했다.
- 2026년 9월 입학 기준에서 비자가 필요한 학생의 지원 마감일 `1 September 2026`만 선택했다.
- 과정 페이지의 추천서 및 Supporting Statement 불필요 문구를 보존하고, 공식 지원 서류 페이지의 CV를 `Optional`로 처리했다.
- Sheffield 실제 구조를 재현하는 reader-parser 통합 테스트와 사이트 등록 테스트를 추가했다.

## Decisions - Sheffield 집중 지원 1차

- 학교별 reader를 복제하지 않고, 사이트 등록부에 공식 URL을 설정하고 generic reader가 같은 출처의 보조 페이지를 읽는 방식으로 구현했다. 이 구조는 다른 학교의 분산형 안내에도 재사용할 수 있다.
- 일반 마감일 `4 September 2026`보다 J님의 실제 학생 조건에 맞는 비자 필요 학생 마감일 `1 September 2026`을 우선했다.
- 과정별 안내가 추천서와 Supporting Statement를 요구하지 않는다고 명시하므로, 대학 전체 지원 서류 페이지의 일반 안내보다 과정 페이지를 우선했다.
- CV의 `may wish to supply`는 필수가 아니므로 `Optional`로 표시한다.

## Current Status - Sheffield 집중 지원 1차

- 실제 과정 페이지와 한국 학력, 지원비, 마감일, CV 공식 페이지에서 대상 문구와 표 구조를 확인했다.
- 실제 페이지 console 경고 및 오류는 없었다.
- 집중 테스트 12개와 전체 회귀 검사 88개가 모두 통과했다.
- 로컬 확장 프로그램은 아직 커밋하거나 배포하지 않았다.

## Next Steps - Sheffield 집중 지원 1차

- 전체 회귀 검사 통과 후 Chrome 확장 프로그램을 다시 로드하고 Sheffield 과정 페이지를 새로고침한다.
- Side Panel에서 9개 결과 항목이 예상값과 일치하는지 최종 현장 확인한다.

## Work Summary - Warwick General Entry Requirements 제목 인식

Warwick 학비 수집 수정 후 Entry Requirements가 비어 보이는 문제를 실제 페이지 탭 상태와 DOM으로 재검증했다. 숨겨진 탭 본문은 정상적으로 읽을 수 있었고, 원인은 Warwick의 섹션 제목 `General entry requirements`가 generic reader의 기존 `Entry requirements` 제목 패턴에 포함되지 않았기 때문이었다.

## Work Completed - Warwick General Entry Requirements 제목 인식

- Entry Requirements 제목 패턴이 `General entry requirements`를 허용하도록 확장했다.
- 기존 Warwick 동적 국가 선택 테스트에서 Entry Requirements의 `2:1 undergraduate degree` 수집을 함께 검증하도록 보강했다.
- 같은 테스트에서 `English language requirements`가 Entry Requirements 값에 섞이지 않는 것도 검증했다.
- 실제 Warwick 페이지에서 Entry Requirements 탭을 활성화하고 동일한 섹션 경계로 원문을 읽어 결과를 확인했다.

## Decisions - Warwick General Entry Requirements 제목 인식

- 숨겨진 탭 전체를 `textContent`로 강제 수집하면 다른 탭의 정보가 섞일 위험이 있으므로 적용하지 않았다.
- 학교 전용 reader를 추가하지 않고 일반적인 제목 변형인 `General`만 공통 제목 패턴에 추가했다.
- Entry Requirements는 `Minimum requirements`와 학력 문장까지만 포함하고, English language부터는 기존 공통 경계 규칙으로 분리했다.

## Current Status - Warwick General Entry Requirements 제목 인식

- 실제 페이지 추출 결과: `Minimum requirements 2:1 undergraduate degree (or equivalent)...`.
- 실제 추출 결과에 `English language requirements`가 포함되지 않음을 확인했다.
- 브라우저 console 경고·오류 없음. Warwick 페이지는 Entry Requirements 탭이 활성화된 상태로 복원했다.
- generic reader 집중 테스트 10개와 전체 회귀 검사 87개가 모두 통과했다.

## Next Steps - Warwick General Entry Requirements 제목 인식

- 전체 회귀 검사를 통과한 로컬 확장을 Chrome에서 다시 로드하고 Warwick 과정 페이지를 새로고침한다.
- Side Panel에서 Entry Requirements, Korean Equivalent, Tuition Fee 세 항목이 동시에 정상 표시되는지 확인한다.

## Work Summary - Warwick 공통 학비표 과정 행 선택

Warwick 과정 페이지의 공통 학비 링크를 따라간 generic reader가 전체 대학원 과정 표의 Overseas 행을 모두 학비 후보로 수집하던 문제를 수정했다. 실제 과정 페이지와 공통 학비표를 비교해 과정 코드 `P-N500`이 안정적인 연결 키임을 확인했고, 현재 작업 기준과 정확히 일치하는 한 행과 한 학년도 열만 읽도록 변경했다.

## Work Completed - Warwick 공통 학비표 과정 행 선택

- 과정 페이지 본문에서 `Course code`를 구조화해 읽도록 generic reader를 확장했다.
- 하이픈 학년도 표기인 `2026-27`도 앱 기준인 `2026/27`로 정규화하도록 보완했다.
- 공통 학비표가 `Course Code`, `Course Intensity`, `Fee Status`, 학년도 열을 제공하면 과정 코드·수업 형태·학비 지위·학년도를 모두 대조하도록 했다.
- 현재 기준과 일치하는 행을 찾지 못한 공통 과정 목록에서는 전체 표의 금액을 fallback 후보로 수집하지 않도록 차단했다.
- Warwick 실제 구조를 재현해 `P-N500 + Full Time + Overseas + 2026-27`에서 `£37,450` 하나만 반환하는 테스트를 추가했다.

## Decisions - Warwick 공통 학비표 과정 행 선택

- 과정명은 `Marketing & Strategy`와 `Marketing and Strategy`처럼 페이지마다 달라질 수 있으므로 과정 코드의 완전 일치를 우선 키로 사용했다.
- 과정 코드만 맞아도 학비를 확정하지 않고 학년도, 수업 형태, Fee Status까지 모두 맞아야 후보로 반환하도록 했다.
- 공통 목록 구조를 이해하지 못하거나 현재 과정과 매칭하지 못하면 임의 금액을 보여주지 않고 기존 공식 학비 링크 후속 확인 상태를 유지하도록 fail closed로 처리했다.

## Current Status - Warwick 공통 학비표 과정 행 선택

- 실제 Warwick 과정 페이지에서 과정 코드 `P-N500`을 확인했다.
- 실제 공통 학비표에서 `Marketing and Strategy (MSc) / P-N500 / Full Time / Overseas / 2026-27 / £37,450` 행을 확인했다.
- 실제 페이지와 공통 학비표 모두 관련 브라우저 경고·오류가 없고 framework 오류 화면이 없음을 확인했다.
- `npm.cmd run check`: 문법 검사와 전체 87개 테스트 통과.
- `git diff --check`: 공백 오류 없음. 기존 줄바꿈 형식 경고만 존재한다.

## Next Steps - Warwick 공통 학비표 과정 행 선택

- Chrome 확장 프로그램 관리 화면에서 로컬 확장을 다시 로드하고 Warwick 과정 페이지를 새로고침한다.
- Side Panel의 Tuition Fee가 다수 후보가 아니라 `£37,450` 한 개로 표시되는지 확인한다.
- Warwick의 나머지 지원 항목에서 실제 업무를 방해하는 오인식이 있는지 다음 실사용 결과를 기준으로 진행한다.

## Work Summary - Warwick 한국 학력 요건 자동 선택

Warwick 과정 페이지에서 한국 학력 요건을 찾지 못하던 문제를 실제 페이지 HTML과 공개 국가별 입학요건 API로 확인했다. Warwick은 일반 `<select>`를 사용하지만 국가명을 `Korea (South)`로 표기하고, 국가 목록과 선택 결과를 모두 JavaScript로 늦게 생성한다. 이를 generic reader의 재사용 가능한 동적 선택 기능으로 지원했다.

## Work Completed - Warwick 한국 학력 요건 자동 선택

- South Korea 별칭에 `Korea (South)`를 추가했다.
- 자동 분석 시 국가 옵션이 늦게 생성되어도 최대 4초 동안 옵션을 기다린 뒤 선택하도록 generic reader를 보강했다.
- 제목 없이 별도 DOM 영역에 삽입되는 국가별 결과를 읽을 수 있도록 설정 기반 결과 selector를 추가했다.
- Warwick 과정 URL을 generic reader 기반 지원 학교로 등록하고 `#international-content`를 한국 학력 결과 영역으로 지정했다.
- Side Panel이 학교별 결과 selector 설정을 generic reader에 전달하도록 연결했다.
- 동적으로 생성되는 Warwick 옵션과 한국 GPA 결과를 재현하는 회귀 테스트 및 사이트 등록 테스트를 추가했다.

## Decisions - Warwick 한국 학력 요건 자동 선택

- Warwick 전용 reader를 새로 만들지 않고, 여러 학교에서 재사용할 수 있는 `동적 옵션 대기 + 설정된 결과 영역 읽기` 기능을 generic reader에 추가했다.
- 국가 선택 결과 영역의 DOM selector만 사이트 설정으로 두었다. 국가별 콘텐츠와 GPA 값은 코드에 고정하지 않고 현재 페이지가 제공하는 원문을 그대로 읽는다.
- 실제 공개 API에서 `Korea (South)`의 2:1 기준이 4.3 만점 3.0, 4.5 만점 3.5로 제공되는 것을 확인했지만, 이 숫자는 코드에 저장하지 않았다.

## Current Status - Warwick 한국 학력 요건 자동 선택

- `npm.cmd run check`: 문법 검사와 전체 86개 테스트 통과.
- `git diff --check`: 공백 오류 없음. 기존 줄바꿈 형식 경고만 존재한다.
- 실제 Warwick 페이지 원문에서 동적 목록 API와 `#international-content` 삽입 구조를 확인했다.
- 로컬 코드 수정은 완료됐으며, 설치된 Chrome 확장 프로그램을 다시 로드한 뒤 실제 Side Panel 결과 확인이 필요하다.

## Next Steps - Warwick 한국 학력 요건 자동 선택

- Chrome 확장 프로그램 관리 화면에서 로컬 확장을 다시 로드하고 Warwick 페이지를 새로고침한다.
- 첫 자동 분석에서 드롭다운이 `Korea (South)`로 바뀌고 Korean Equivalent에 2:1 기준 3.0/4.3 및 3.5/4.5가 표시되는지 확인한다.
- 별도로 보고된 Warwick 학비 후보 과다 수집 문제는 한국 학력 자동 선택 확인 후 다음 작업으로 분리해 수정한다.

## Work Summary

Edinburgh 과정 페이지에서 Entry Requirements가 학력 요건뿐 아니라 국가 선택 목록, 지원 참고사항, 미래 학년도 내용까지 과도하게 수집되는 문제를 수정했다. 원인은 특정 국가의 사이트 구조가 아니라 generic reader가 아코디언 내부 전체 컨테이너를 하나의 원문으로 읽는 공통 섹션 경계 오류였다.

## Work Completed

- `read-generic-page.js`에 학력 요건 제목 패턴과 학력 블록 수집 로직을 추가했다.
- `Qualifications` 블록을 우선 선택하고, 해당 블록이 없을 때 `Academic requirements`, `Standard requirements`, `Programme specific requirements` 등의 학력 요건 블록을 제한적으로 선택하도록 했다.
- `International qualifications` 국가 선택 목록과 별도 미래 학년도 아코디언 내용이 Entry Requirements 원문에 섞이지 않도록 범위를 좁혔다.
- Edinburgh형 아코디언 구조를 재현하는 generic reader 회귀 테스트를 추가했다.

## Decisions

- `limit()`으로 텍스트 길이만 자르는 방식은 문제의 원인인 과잉 수집을 해결하지 못하므로, 문자열 길이보다 DOM 의미 단위인 학력 요건 카드와 제목을 우선했다.
- 국가별 adapter를 새로 만들지 않고 generic reader의 공통 경계를 수정했다. Edinburgh와 호주에서 같은 증상이 재현될 수 있는 공통 버그였기 때문이다.

## Current Status

- 정적 구문 검사와 기존 테스트, 새 generic reader 회귀 테스트를 실행했고 69개 테스트가 모두 통과했다.
- 실제 Edinburgh 페이지 DOM에 수정된 reader를 적용해 Entry Requirements 결과가 747자로 수집되는 것을 확인했다. `2:1` 학력 요건은 유지되고 국가 목록과 `2027-28` 문자열은 제외됐다.
- 파싱된 Entry Requirements 필드와 복사 원문도 동일한 747자 결과로 확인했다.
- 아직 모든 학교의 Entry Requirements 구조를 검증한 것은 아니며, `Qualifications`라는 제목을 사용하지 않는 generic 사이트는 기존 fallback 수집 규칙이 적용된다.

## Next Steps

- 실제 Chrome 확장 화면을 새 코드로 다시 로드한 뒤 Edinburgh Entry Requirements 카드의 표시와 복사 결과를 확인한다.
- 다른 generic 학교에서 학력 요건 블록이 정상적으로 유지되는지 확인한다.

## Work Summary - 다중 공식 출처와 연결 학비

CSV에 정리된 대표 학교군에서 반복되는 “과정 페이지에는 학비가 없고 별도 공식 링크에 있음” 문제를 먼저 공통 기능으로 흡수했다. 학교별 예외를 추가하기 전에 Edinburgh 구조를 대표 사례로 삼아 링크 발견과 후속 학비 표 판독을 generic reader에 연결했다.

## Work Completed - 다중 공식 출처와 연결 학비

- 같은 학교 도메인의 학비 관련 링크를 최대 3개까지 발견하고 snapshot에 `tuitionFeeLinks`로 보존하도록 했다.
- 링크 페이지를 동일 출처에서 최대 4초 동안 읽고, 표의 `International / EU`, `International`, `Overseas` 등의 열과 학년도 행을 이용해 학비 후보를 만들도록 했다.
- `2026/7`처럼 한 자리로 표시된 학년도도 `2026/27`로 정규화하도록 보강했다.
- 링크를 발견했지만 금액을 확정하지 못하면 기존의 막연한 미발견 대신 `linked_tuition_page` 후속 확인 상태와 공식 링크를 표시하도록 했다.
- Edinburgh형 학비 표와 링크 실패 fallback에 대한 회귀 테스트를 추가했다.

## Decisions - 다중 공식 출처와 연결 학비

- 링크 추적은 임의의 외부 링크가 아니라 현재 과정 페이지와 같은 도메인의 학비 관련 링크로 제한했다.
- 후속 페이지가 느리거나 응답하지 않아도 전체 분석이 멈추지 않도록 4초 timeout과 수동 확인 fallback을 사용했다.
- 이번 단계에서는 모든 지원 안내 페이지를 한꺼번에 통합하지 않고, CSV에서 반복성이 높고 Edinburgh에서 즉시 검증 가능한 Tuition 링크·표부터 공통화했다.

## Current Status - 다중 공식 출처와 연결 학비

- `npm.cmd test`: 71개 테스트 통과.
- `npm.cmd run check`: 정적 검사와 전체 테스트 통과.
- 회귀 테스트에서 Edinburgh 링크 페이지의 `£33,200.00`, `2026/7 → 2026/27`, 원문 URL 보존을 확인했다.
- 실제 Edinburgh 페이지에서 학비 링크 발견은 확인했다. 브라우저 검증 환경의 읽기 전용 평가에서는 `fetch`가 제공되지 않아 실제 링크 페이지 자동 fetch 결과까지는 확인하지 못했으며, 이 부분은 확장 프로그램을 다시 로드한 뒤 실제 Side Panel 분석으로 최종 확인해야 한다.

## Next Steps - 다중 공식 출처와 연결 학비

- Chrome에서 확장 프로그램을 다시 로드하고 Edinburgh 과정 페이지를 분석해 학비 후보가 자동으로 `£33,200.00`으로 표시되는지 확인한다.
- 이후 UCL·Leeds·Warwick의 별도 지원 안내/학비 링크를 같은 source-link 흐름으로 연결한다.
- 다음 공통 기능은 국가 선택형 Korean Equivalent와 `How to apply`의 Reference·SOP·CV 탐색으로 진행한다.

## Work Summary - 국가 선택과 지원 안내 출처

대표 학교군에서 반복되는 드롭다운 국가 선택과 `How to apply`·`Application process` 별도 안내 문제를 generic reader가 후속 확인 대상으로 보존하도록 확장했다. 자동으로 국가를 선택하거나 내용을 추측하지 않고, 사용자가 확인해야 하는 상태와 공식 출처를 분리해 표시하는 방향을 선택했다.

## Work Completed - 국가 선택과 지원 안내 출처

- South Korea, Republic of Korea, Korea, Republic of 등 국가 옵션을 가진 `<select>`를 감지해 `koreanAcademicRequirementSelection`으로 보존했다.
- 별도 국제 학력 요건, Application Fee, Application Deadline, Reference, SOP, CV 관련 같은 도메인 링크를 snapshot에 기록했다.
- Korean Equivalent는 국가 선택 필요·별도 국제 학력 페이지·기존 원문 발견을 서로 다른 후속 상태로 처리하도록 했다.
- Application Fee, Application Deadline, Reference, SOP, CV도 링크만 발견된 경우 단순 누락 대신 공식 출처를 여는 `ACTION_REQUIRED` 결과를 반환하도록 했다.
- 국가 옵션·지원 안내 링크·parser 후속 상태에 대한 회귀 테스트를 추가했다.

## Decisions - 국가 선택과 지원 안내 출처

- 페이지를 자동으로 조작해 South Korea를 선택하지 않았다. 학교별로 선택 변경이 POST, AJAX, 전체 페이지 이동을 일으킬 수 있으므로 현재 단계에서는 데이터 오염과 예기치 않은 이동을 피하고 명시적 확인 상태를 사용한다.
- 관련 링크는 현재 페이지와 같은 도메인으로 제한했다. 외부 도메인 링크는 다음 source permission과 검증 규칙을 정한 뒤 별도로 다룬다.
- `How to apply` 하나가 Reference·SOP·CV를 모두 안내할 수 있으므로 링크를 항목별로 복제하지 않고, 각 항목의 후속 확인 출처로 재사용한다.

## Current Status - 국가 선택과 지원 안내 출처

- `npm.cmd test`: 73개 테스트 통과.
- `npm.cmd run check`: 정적 검사와 전체 테스트 통과.
- 현재 generic reader는 학비 링크를 같은 도메인에서 자동 판독하고, 나머지 관련 페이지는 공식 링크와 후속 확인 상태까지 연결한다.
- 실제 자동 국가 선택과 여러 지원 안내 페이지의 본문 통합은 아직 구현하지 않았다. 현재는 사용자가 원문을 열어 확인할 수 있는 안전한 중간 단계다.

## Next Steps - 국가 선택과 지원 안내 출처

- Chrome에서 UCL·Leeds·Warwick 대표 페이지를 다시 분석해 국가 선택, `How to apply`, 별도 학력 요건 링크가 예상대로 표시되는지 확인한다.
- 반복되는 동일 도메인 후속 페이지에 한해 Tuition과 같은 제한적 one-hop 본문 판독을 추가한다.
- `not_required` 문구와 Rolling/staged admission을 별도 상태로 확장한다.

## Work Summary - 결과 항목 기본 노출 순서 조정

정상적으로 추출된 항목이 `나머지 확인 항목` 안으로 접혀 첫 화면에서 보이지 않는 문제를 수정했다. 모든 결과 카드를 기본 화면에 꺼내고, 지원서 핵심 정보와 보조 서류 정보를 가로선으로 구분했다.

## Work Completed - 결과 항목 기본 노출 순서 조정

- `Entry Requirements`, `Korean Equivalent`, `IELTS`, `Tuition Fee`, `Application Fee`, `Application Deadline`을 첫 번째 그룹에 고정했다.
- `Application Deadline` 다음에 실제 `hr` 가로 구분선을 추가하고, `Reference`, `SOP Guideline`, `CV`를 두 번째 그룹으로 항상 표시했다.
- 상태가 정상인지, 메모가 있는지에 따라 카드를 접던 기존 분류 로직과 `나머지 확인 항목` 접이식 UI를 제거했다.
- 정렬 기준을 항목의 `order`로 명시해 분석 결과가 반환되는 순서와 관계없이 요청한 순서를 유지하도록 했다.
- 정적 UI 하네스와 화면 구조 테스트를 새 두 그룹 계약에 맞게 갱신했다.
- 현재 동작과 달랐던 `ROADMAP.md`의 기본 화면 설명도 두 그룹 전체 노출 기준으로 갱신했다.

## Decisions - 결과 항목 기본 노출 순서 조정

- 첫 번째 그룹의 마지막 항목 다음에 구분선을 두기 위해 `reference`를 그룹 분리 기준으로 사용했다. 기존 `FIELDS`의 도메인 순서를 그대로 활용하므로 라벨이나 추출 데이터 계약은 변경하지 않았다.
- `Entry Requirements`와 `Application Deadline` 등 기존 영문 라벨은 데이터·화면 계약과 일치하므로 유지했다.

## Current Status - 결과 항목 기본 노출 순서 조정

- `npm.cmd run check`를 실행해 문법 검사와 전체 69개 테스트가 모두 통과했다.
- 브라우저에서 로컬 `file://` 하네스를 열어 시각 검증하려 했으나 브라우저 URL 정책이 파일 주소를 차단해 스크린샷 기반 검증은 완료하지 못했다. HTML 하네스와 구조 테스트로 카드 순서·구분선·접이식 UI 제거를 확인했다.

## Next Steps - 결과 항목 기본 노출 순서 조정

- 실제 Chrome 확장을 다시 로드한 뒤 J님의 현재 과정 페이지에서 아홉 개 카드가 같은 순서와 구분선으로 표시되는지 확인한다.

## Work Summary - 부정 조건과 비일자형 일정

학교 페이지가 지원 서류를 명시적으로 요구하지 않거나, 지원 일정을 날짜가 아닌 rolling·staged 방식으로 안내하는 경우를 단순 누락과 구분했다. Sheffield의 “references를 요구하지 않음” 사례와 Edinburgh/UCL 계열에서 반복될 수 있는 비일자형 일정 표현을 공통 계약으로 흡수해, 학교별 예외를 늘리지 않고 의미 있는 결과를 보존하는 것을 목표로 했다.

## Work Completed - 부정 조건과 비일자형 일정

- `NOT_REQUIRED` 추출 상태와 `요구하지 않음` 표시 메타데이터를 추가했다.
- Reference·SOP·CV 문맥에서 `not required`, `do not require`, `will not ask` 등의 명시적 부정 문구를 찾으면 원문과 함께 `not_required`로 보존하도록 generic reader를 확장했다.
- 파서는 `not_required`를 정상적으로 해석하고 복사 대상으로 만들지 않도록 했다. Side Panel 요약에서도 이를 다시 확인이 필요한 항목으로 세지 않는다.
- `rolling basis`, `staged admission`, `staged entry` 문구를 날짜 후보와 별도로 수집해 `applicationDeadlineModes`로 보존했다.
- 날짜 후보가 없을 때 rolling·staged 일정은 `ACTION_REQUIRED`와 의미 있는 사유 코드로 표시하고 원문 출처를 연결했다.
- 부정 지원 서류, rolling 일정, staged 일정에 대한 generic reader·course parser·Side Panel 회귀 테스트를 추가했다.

## Decisions - 부정 조건과 비일자형 일정

- “요구하지 않음”은 `FOUND`와 다른 의미이므로 별도 상태로 두되, 사용자 관점에서는 해결된 정보로 취급했다. 그래야 실제 누락과 학교의 명시적 미요구를 구분할 수 있다.
- rolling·staged는 날짜가 없다고 `NOT_FOUND`로 처리하지 않았다. 다만 정확한 마감일이나 현재 라운드를 자동 확정하지 않았으므로 `ACTION_REQUIRED`로 남겨 원문 확인을 유도했다.
- 부정 문구는 해당 지원 서류 이름과 같은 콘텐츠 블록 안에 명시적 부정 표현이 있을 때만 인식했다. 다른 항목의 “not required” 문구가 Reference·SOP·CV 결과를 오염시키지 않도록 보수적으로 제한했다.

## Current Status - 부정 조건과 비일자형 일정

- `npm.cmd test`: 77개 테스트 통과.
- `npm.cmd run check`: 문법 검사와 전체 테스트 통과.
- 현재 변경은 generic reader와 공통 parser 계약을 확장한 것이며, Manchester·KCL·QMUL 같은 기존 정밀 adapter의 동작은 직접 수정하지 않았다.
- 실제 Chrome Side Panel에서 Sheffield의 문구와 학교별 rolling·staged 페이지를 재분석하는 현장 확인은 아직 남아 있다.

## Next Steps - 부정 조건과 비일자형 일정

- Chrome에서 Sheffield를 먼저 열어 Reference·SOP·CV가 `요구하지 않음`으로 표시되는지 확인한다.
- Edinburgh에서 staged admission 안내가 날짜 후보와 함께 있을 때 날짜를 우선 보존하고, 날짜가 없을 때만 staged 상태로 떨어지는지 확인한다.
- 이후 15개 학교 중 같은 도메인에서 반복되는 `How to apply` 본문을 one-hop으로 읽을지, 현재처럼 링크 후속 확인으로 둘지 실제 사용 시간을 기준으로 결정한다.

## Work Summary - Entry Requirements 플랫 구조 경계 보강

기존 Entry Requirements 수정이 내부 하위 제목이나 카드 구조를 가진 학교에서는 작동했지만, UCL·Sheffield처럼 본문 단락 다음에 English language, 국제 학력, 기타 요구사항이 같은 섹션 흐름으로 이어지는 페이지에서는 다시 전체 정보를 과수집할 수 있는 문제가 확인됐다. generic reader가 하위 제목 존재 여부에만 의존하지 않도록 학력 본문과 다음 영역을 구분하는 경계를 추가했다.

## Work Completed - Entry Requirements 플랫 구조 경계 보강

- `Supporting your application`, `English language requirements`, `Equivalent qualifications`, `International qualifications`, `Other requirements`, `Fees`, `How to apply` 등 다음 영역의 시작 표현을 공통 경계 패턴으로 정의했다.
- 카드·아코디언 내부 텍스트를 읽을 때도 해당 경계 이후의 내용을 잘라 학력 요건만 남기도록 했다.
- 하위 제목이 없는 플랫한 형제 요소를 순회할 때 경계 표현을 만나는 즉시 중단하도록 했다.
- UCL형 단락·버튼 구조와 기존 Edinburgh형 카드 구조에 대한 회귀 검사를 보강했다.

## Decisions - Entry Requirements 플랫 구조 경계 보강

- 학교별 adapter를 추가하지 않고 generic reader의 “학력 본문 종료 지점”을 개선했다. 여러 학교에서 반복되는 과수집 원인이 DOM 모양 자체보다 공통 섹션 경계 처리에 있었기 때문이다.
- English language 조건은 별도 `IELTS` 필드가 담당하므로 Entry Requirements에 포함하지 않는다. 국제 학력 선택 UI도 Korean Equivalent가 담당하므로 Entry Requirements에서 제외한다.
- 경계 표현을 발견하지 못한 사이트는 기존 fallback을 사용하되, 실제 학교에서 실패한 문구를 다음 회귀 테스트에 추가하는 방식으로 확장한다.

## Current Status - Entry Requirements 플랫 구조 경계 보강

- `npm.cmd test`: 78개 테스트 통과.
- `npm.cmd run check`: 문법 검사와 전체 테스트 통과.
- UCL형 플랫 구조에서 학력 단락은 유지하고 IELTS·국제 학력 안내는 Entry Requirements에서 제외하는 회귀 테스트를 확인했다.
- 실제 Chrome에서 UCL·Leeds·Bristol·Sheffield를 다시 분석하는 현장 검증은 아직 남아 있다.

## Next Steps - Entry Requirements 플랫 구조 경계 보강

- 실제 Chrome에서 15개 학교 중 과수집이 보고된 UCL·Leeds·Bristol·Sheffield를 먼저 확인한다.
- 각 학교에서 여전히 과수집되는 고유 경계 표현만 기록해 generic 경계 패턴을 보강한다.
- Warwick의 학비 100개 후보 문제는 Entry Requirements와 분리된 후보 폭발 문제이므로 다음 작업으로 별도 수정한다.

## Work Summary - Entry Requirements 문장 내부 오탐 수정

플랫 구조 경계를 보강한 뒤 `international qualification`처럼 실제 학력 요건 문장 안에 포함된 단수 표현을 다음 섹션 제목으로 잘못 인식하는 회귀가 발견됐다. 제공된 UCL형 문장에서 `(or international qualification...)` 직후 잘리는 현상을 기준으로 경계 패턴을 다시 좁혔다.

## Work Completed - Entry Requirements 문장 내부 오탐 수정

- `international qualification`, `equivalent qualification`, `other requirement` 같은 일반 문장형 단수 표현은 Entry Requirements 경계로 사용하지 않도록 수정했다.
- `International qualifications`, `Equivalent qualifications`, `Other requirements`처럼 실제 섹션 라벨에서 사용되는 복수형 표현만 경계로 유지했다.
- 제공된 문장의 `or international qualification` 이후 본문과 `There is not an exhaustive list` 문장이 계속 보존되는 회귀 테스트를 추가했다.

## Decisions - Entry Requirements 문장 내부 오탐 수정

- 학력 본문은 문장 내용보다 섹션 라벨을 우선해 잘라야 한다. 학력 요건에서 `international qualification`은 흔한 정상 표현이므로 단순 키워드 포함만으로 종료시키면 안 된다.

## Current Status - Entry Requirements 문장 내부 오탐 수정

- 이번 수정 후 전체 테스트와 정적 검사를 다시 실행한다.
- 실제 Chrome 확장에 반영하려면 최신 로컬 폴더를 다시 로드해야 하며, 이전 ZIP을 재설치한 경우에는 이 수정이 포함되지 않는다.

## Work Summary - UCL 1차 지원

15개 학교 중 첫 번째 대표 학교로 UCL을 선택하고, UCL 전용 수집기를 별도로 만들지 않는 범위에서 현재 generic reader와 site registry를 확장했다. 실제 UCL 과정 페이지의 국가 선택과 접힌 영어 요건 구조를 확인한 뒤, UCL 프로필에만 자동 상호작용을 허용했다.

## Work Completed - UCL 1차 지원

- UCL 과정 URL을 `ucl` site profile로 등록하고 generic reader에 연결했다.
- UCL의 `English language requirements` 버튼을 자동으로 펼친 뒤 `Level 2` 또는 `Band B` 같은 핵심 값을 읽을 수 있게 했다.
- UCL의 국가 선택 `<select>`에서 `South Korea` 옵션을 찾으면 `KR` 값을 선택하고 input/change 이벤트를 발생시킨 뒤 페이지를 다시 읽도록 했다.
- 국가 선택 후 생성되는 `Equivalent qualifications for South Korea` 제목과 한국 학력 본문을 Korean Equivalent 수집 대상으로 연결했다.
- UCL 프로필에만 `autoSelectCountry`와 `expandEnglishAccordion`을 켜고, 다른 generic 학교는 기존 수동 확인 흐름을 유지했다.
- UCL site registry·generic reader 회귀 테스트를 추가했다.

## Decisions - UCL 1차 지원

- UCL의 DOM 구조는 실제 페이지에서 확인한 뒤 공통 reader의 옵션으로 흡수했다. UCL만을 위한 별도 전체 parser를 만들지 않고, 다른 학교에도 적용 가능한 “선택적으로 허용된 페이지 상호작용”으로 모델링했다.
- 자동 국가 선택은 모든 학교에 일괄 적용하지 않는다. 선택 변경이 AJAX·페이지 이동을 일으킬 수 있으므로 site profile이 명시한 학교에서만 사용한다.
- UCL 한국 학력 결과는 선택 전의 국가 목록이 아니라 선택 후 생성되는 `Equivalent qualifications for South Korea` 본문을 근거로 삼는다.

## Current Status - UCL 1차 지원

- 실제 UCL 페이지에서 국가 select의 name이 `ucl_international_equivalencies`, South Korea option value가 `KR`임을 확인했다.
- 실제 선택 후 `Equivalent qualifications for South Korea`와 `Bachelor's degree with a minimum final CGPA of 3.5/4.5 or 3.3/4.3` 본문이 나타나는 것을 확인했다.
- 영어 요건 아코디언을 펼치면 `The English language level for this course is: Level 2`가 나타나는 것을 확인했다.
- `npm.cmd test`: 79개 테스트 통과.
- `npm.cmd run check`: 정적 검사와 전체 테스트 통과.
- 실제 확장 Side Panel에서 UCL 전체 분석을 실행하는 최종 현장 검증은 아직 남아 있다.

## Next Steps - UCL 1차 지원

- 최신 로컬 확장을 다시 로드하고 UCL 과정 페이지에서 Entry·Korean·English 카드가 실제 분석 결과로 표시되는지 확인한다.
- UCL의 Next steps에 있는 Application Fee·Reference·SOP·CV·Deadline을 공식 링크 또는 본문으로 연결하는 2차 작업을 진행한다.
- UCL 검증이 끝난 뒤 Warwick으로 이동해 학비 후보 100개 문제를 별도 해결한다.

## Work Summary - UCL 2차 지원 항목

UCL 과정 페이지의 `Next steps`에서 Application Fee, 비자 여부별 Deadline, Reference, SOP 안내를 직접 읽고, 과정 페이지에서 확정할 수 없는 CV는 공식 지원 안내 링크로 보존하도록 generic reader와 공통 parser를 확장했다.

## Work Completed - UCL 2차 지원 항목

- `application processing fee` 표현을 Application Fee 후보로 인식해 UCL의 `£90`을 수집하도록 했다.
- UCL 프로필에서만 `Applicants who require a visa`와 `Applicants who do not require a visa` 날짜 범위를 읽어 각 종료일을 별도 후보로 저장하도록 했다.
- 비자 여부별 마감일 후보가 둘이면 하나를 임의 선택하지 않고 `multiple_deadline_categories` 상태와 두 날짜를 함께 표시하도록 parser를 수정했다.
- `[role='alert']`에 들어 있는 `This course requires two references` 문구도 지원 서류 근거로 읽도록 content block 범위를 보완했다.
- `When we assess your application we would like to learn` 다음 목록과 personal statement 설명을 SOP 지침으로 묶어 보존하도록 했다.
- CV 요구 여부를 과정 페이지에서 확정하지 못한 경우 `Application Guidance` 공식 링크를 후속 확인 근거로 남기도록 했다.
- UCL 실제 DOM 형태와 parser 결과를 각각 검증하는 회귀 테스트를 추가했다.

## Decisions - UCL 2차 지원 항목

- 비자 필요 여부에 따라 마감일이 다른 값은 단일 Deadline으로 자동 확정하지 않는다. J님이 지원자 조건을 확인한 뒤 고를 수 있도록 두 후보와 의미를 함께 보여준다.
- 과정 페이지에 CV 필수 문구가 없다는 사실을 `Not required`로 해석하지 않는다. UCL의 일반 지원 안내에서 CV를 제출할 수 있다는 설명과 과정별 필수 여부는 다르므로, 공식 링크를 연결한 `ACTION_REQUIRED`가 안전하다.
- UCL 전용 parser를 새로 만들지 않고, 사이트 프로필이 활성화하는 비자별 날짜 수집과 공통적인 지원 문서 구조 인식을 generic reader에 추가했다.

## Current Status - UCL 2차 지원 항목

- 실제 UCL Computational Finance MSc 페이지에서 `Next steps`, `£90`, `two references`, 비자 필요·불필요 두 일정이 현재 DOM에 존재함을 확인했다.
- 실제 페이지의 브라우저 console warning/error는 확인되지 않았다.
- `npm.cmd test`: 81개 테스트 통과.
- `npm.cmd run check`: 문법 검사와 전체 81개 테스트 통과.
- `git diff --check`: 공백 오류 없음. 줄바꿈 변환 경고만 존재한다.
- 최신 로컬 확장을 Chrome에서 다시 로드한 뒤 Side Panel 결과 카드까지 확인하는 현장 검증은 아직 남아 있다.

## Next Steps - UCL 2차 지원 항목

- Chrome 확장 관리 화면에서 최신 로컬 폴더를 다시 로드하고 UCL Side Panel의 Application Fee·Deadline·Reference·SOP·CV 표시를 확인한다.
- UCL 결과가 확인되면 다음 대표 학교인 Warwick으로 이동해 학비 후보가 과다 수집되는 문제를 해결한다.

## Work Summary - UCL 학비 보증금 오탐과 마감일 범위 수정

UCL Strategic Management of Projects MSc에서 Tuition Fee가 `£50,600 / £350` 두 후보로 표시되고, 비자가 필요하지 않은 학생의 마감일까지 함께 수집되는 문제를 실제 과정 페이지와 연결된 UCL 공통 학비 페이지에서 재현했다.

## Work Completed - UCL 학비 보증금 오탐과 마감일 범위 수정

- `£350`의 출처가 과정 학비가 아니라 UCL 공통 Tuition Fees 페이지의 UK flexible/modular offer holder용 보증금임을 확인했다.
- 과정 페이지에서 직접 국제학생 학비를 찾은 경우에는 공통 학비 링크를 추가로 따라가지 않도록 linked tuition 수집을 fallback 전용으로 변경했다.
- 직접 학비가 없는 Edinburgh형 페이지는 기존처럼 한 단계 연결된 학비 표를 읽도록 유지했다.
- UCL 프로필의 마감일 옵션을 `captureVisaRequiredDeadline`으로 좁히고 `Applicants who require a visa` 일정만 수집하도록 했다.
- 과정 학비를 찾은 뒤 공통 페이지 보증금을 추가하지 않는 회귀 테스트와 UCL 비자 필요 마감일 단일 확정 테스트를 추가·수정했다.

## Decisions - UCL 학비 보증금 오탐과 마감일 범위 수정

- 과정 페이지의 직접 학비는 연결된 공통 안내 페이지보다 우선한다. 직접 근거가 있으면 one-hop 탐색을 중단해 보증금·생활비·일반 범위 금액이 학비 후보로 섞이지 않게 한다.
- 이 앱의 대상 학생은 비자가 필요하므로 UCL의 `No visa required` 일정은 후보로 보존하지 않는다.
- `£350`이나 `£50,600` 같은 숫자를 학교별로 고정하지 않고, fallback 진입 조건을 수정해 같은 유형의 오탐을 일반적으로 차단한다.

## Current Status - UCL 학비 보증금 오탐과 마감일 범위 수정

- 실제 UCL 과정 페이지에서 International students 탭의 Full-time 학비가 `£50,600`이고 Part-time 학비가 `£25,300`임을 확인했다.
- 실제 UCL 공통 Tuition Fees 페이지에서 `£350`이 UK flexible/modular 보증금 문구에 포함된 것을 확인했다.
- 브라우저 검증 중 `International students` 텍스트가 두 곳에 있어 최초 locator가 중복되었고, `#fees-funding`으로 범위를 좁혀 실제 탭 전환을 검증했다.
- 실제 페이지에서 과정명·국제학생 학비·비자 필요 일정이 존재하고, framework overlay와 console warning/error가 없음을 확인했다.
- `npm.cmd test`: 82개 테스트 통과.
- `npm.cmd run check`: 문법 검사와 전체 82개 테스트 통과.
- `git diff --check`: 공백 오류 없음. 줄바꿈 변환 경고만 존재한다.

## Next Steps - UCL 학비 보증금 오탐과 마감일 범위 수정

- Chrome 확장 관리 화면에서 최신 로컬 폴더를 다시 로드한 뒤 Side Panel에 Tuition Fee `£50,600`과 Deadline `27 Mar 2026`만 표시되는지 확인한다.

## Work Summary - 근거 위치 이동과 강조

Side Panel의 수집 결과 아래에 반복 표시되는 근거 텍스트를 단순 참고 문구가 아니라 실제 원문 위치로 이동하는 컨트롤로 바꿨다. 같은 페이지의 근거를 클릭하면 왼쪽 학교 페이지에서 해당 문단으로 스크롤하고 잠시 강조해, 결과와 원문을 빠르게 대조할 수 있게 했다.

## Work Completed - 근거 위치 이동과 강조

- 근거 URL에서 hash와 끝 `/`을 정규화해 현재 탭과 같은 페이지인지 판정하도록 했다.
- 같은 페이지이며 발췌문이 있는 근거는 `왼쪽에서 보기` 버튼으로 표시하도록 Side Panel을 수정했다.
- 근거 발췌문·섹션명과 페이지의 문단, 목록, 표 행, 알림, 제목, 컨테이너 텍스트를 비교해 가장 가까운 DOM 요소를 찾는 `focusSourceInPage`를 추가했다.
- 찾은 요소의 접힌 `<details>` 조상을 펼치고 중앙으로 부드럽게 스크롤한 뒤 키보드 포커스와 노란색 배경·테두리 강조를 약 2.6초간 적용하도록 했다.
- 이동 성공, 검색 실패, 처리 중 상태를 근거 버튼 아래의 접근 가능한 상태 문구로 잠시 표시하도록 했다.
- 다른 페이지 출처와 위치 검색 실패에는 기존 `원문 열기` fallback을 유지했다.
- 근거 매칭·강조·정리 동작과 Side Panel 버튼 계약을 검증하는 테스트 3개를 추가했다.
- UI harness의 근거 영역을 새 버튼 형태로 갱신하고 브라우저에서 펼침 상태와 레이아웃을 확인했다.

## Decisions - 근거 위치 이동과 강조

- 학교별 CSS selector를 저장하지 않고 기존 source의 URL·sectionLabel·excerpt를 텍스트 앵커로 사용한다. 사이트 DOM이 일부 바뀌어도 텍스트가 유지되면 공통 기능이 작동하도록 하기 위한 결정이다.
- 동일 페이지에만 자동 스크롤을 허용한다. 별도 학비·지원 안내 페이지로 임의 이동하면 현재 작업 문맥을 잃을 수 있으므로 다른 URL은 명시적인 `원문 열기`로 남겼다.
- 완전히 일치하지 않는 긴 섹션 발췌문도 표 행이나 개별 문단을 찾을 수 있도록 포함 관계와 단어 겹침을 함께 점수화하되, 근거를 찾지 못하면 낮은 신뢰도로 임의 이동하지 않는다.

## Current Status - 근거 위치 이동과 강조

- `npm.cmd test`: 85개 테스트 통과.
- `npm.cmd run check`: 새 content 함수 문법 검사를 포함한 정적 검사와 전체 85개 테스트 통과.
- `git diff --check`: 공백 오류 없음. 줄바꿈 변환 경고만 존재한다.
- 브라우저 UI harness에서 근거 펼침, `왼쪽에서 보기` 컨트롤, 카드 레이아웃, console error 없음이 확인됐다.
- 실제 UCL 페이지에 브라우저 검증 도구가 직접 DOM 스타일을 삽입하는 것은 제한되어 실제 확장 Side Panel 클릭 검증은 최신 로컬 확장을 다시 로드한 뒤 확인해야 한다.

## Next Steps - 근거 위치 이동과 강조

- Chrome 확장 관리 화면에서 최신 로컬 폴더를 다시 로드하고 UCL의 Entry Requirements·Tuition Fee·Application Deadline 근거 버튼을 각각 눌러 실제 왼쪽 페이지의 이동·강조를 확인한다.
- 실제 사용 중 중복 문장 때문에 잘못된 위치로 이동하는 사례가 발견되면 해당 문구와 섹션명을 회귀 테스트에 추가해 공통 점수 규칙을 보강한다.

## Work Summary - 원문 액션 단순화와 IELTS 출처 분리

결과 카드 안에서 수집값과 같은 근거 전문이 반복되어 화면이 길어지는 문제를 해결했다. IELTS 등급의 근거가 과정 본문에 있는데도 별도 영어 요건 페이지를 출처로 사용해 위치 이동이 실패하던 원인을 데이터 계약에서 분리했다.

## Work Completed - 원문 액션 단순화와 IELTS 출처 분리

- 결과 카드의 `근거` 펼침 영역, 섹션명, 발췌문을 제거하고 `원문 보기` 단일 액션으로 교체했다.
- 같은 페이지 출처는 기존 위치 탐색·스크롤·강조 기능을 사용하고, 다른 페이지 출처는 새 탭 링크로 여는 동작을 유지했다.
- 영어 요건에 `본문 출처 URL`, `본문 근거 텍스트`, `등급 세부 기준 URL`을 분리해 저장하도록 공통 reader·parser 계약을 확장했다.
- UCL의 `Level 2`는 과정 페이지 문장을 근거로 포커스하고, 별도 영어 요건 페이지는 `등급 세부 기준` 링크로 표시하도록 했다.
- KCL의 `English language band: B`와 QMUL의 `Band 4`에도 같은 출처 분리 방식을 적용했다.
- `Level`, `Band`, `CEFR` 등 명시적 등급이 있으면 등급만 표시하고, 명시적 등급 없이 IELTS 점수 문장만 있으면 원문 점수 문장을 유지하도록 generic 수집 규칙을 보강했다.
- 기존 후속 확인 문구의 `근거에서` 표현을 `원문 보기로`로 바꿨다.

## Decisions - 원문 액션 단순화와 IELTS 출처 분리

- 사용자가 대조해야 하는 것은 카드 안의 중복 발췌문이 아니라 실제 학교 페이지이므로 근거 텍스트를 UI에서 제거하고 내부 위치 탐색용 데이터로만 유지한다.
- 영어 등급과 상세 점수표는 역할이 다르다. 과정 페이지는 해당 학과의 등급을 확정하는 근거로, 공통 영어 요건 페이지는 등급 세부 내용을 보는 보조 링크로 취급한다.
- `IELTS 7.0 overall`처럼 학교가 점수를 직접 제시한 경우에는 인위적으로 등급을 추정하지 않고 학교 문장을 그대로 표시한다.

## Current Status - 원문 액션 단순화와 IELTS 출처 분리

- `npm.cmd run check`: 문법 검사와 전체 85개 테스트 통과.
- UCL `Level 2`, KCL `Band B`, QMUL `Band 4`, 일반 IELTS 점수 문장 보존을 회귀 테스트로 확인했다.
- Chrome UI harness에서 `근거` 영역이 사라지고 `원문 보기`와 `등급 세부 기준`이 카드 하단에 겹침 없이 표시되는 것을 확인했다.
- 실제 설치된 확장 프로그램에는 아직 자동 반영되지 않으므로 Chrome 확장 관리 화면에서 최신 로컬 폴더를 다시 로드해야 한다.

## Next Steps - 원문 액션 단순화와 IELTS 출처 분리

- 최신 로컬 확장을 다시 로드한 뒤 UCL IELTS 카드의 `원문 보기`가 과정 본문의 `Level 2` 문장으로 이동하는지 확인한다.
- `등급 세부 기준`이 UCL 영어 요건 페이지를 새 탭으로 여는지 확인한다.
- 다른 학교에서 `Level B1`, `Level B2`, 문자형 Band 등 새로운 표기가 발견되면 해당 실제 문구를 회귀 테스트에 추가한다.

## Work Summary - 출처 없는 카드의 Side Panel 렌더링 오류 수정

`원문 보기` UI를 단순화한 뒤 출처가 없는 항목을 렌더링할 때 `Cannot read properties of null (reading 'url')` 오류가 발생해 Side Panel 렌더링이 중단되는 문제가 확인됐다.

## Work Completed - 출처 없는 카드의 Side Panel 렌더링 오류 수정

- `createSourceActions`에서 nullable `field.source`의 URL을 빈 문자열로 먼저 정규화하도록 수정했다.
- 링크 생성과 세부 기준 URL 비교가 모두 정규화된 `sourceUrl`만 사용하도록 바꿨다.
- 원문 액션 함수 안에서 `source.url`을 직접 읽는 코드가 다시 생기지 않도록 회귀 검사를 추가했다.

## Decisions - 출처 없는 카드의 Side Panel 렌더링 오류 수정

- 출처가 없는 것은 Application Fee 등의 정상적인 `NOT_FOUND` 상태일 수 있으므로 오류로 취급하지 않는다. 해당 카드는 원문 링크 없이 나머지 상태와 안내 문구를 그대로 렌더링한다.
- 조건문 일부에만 optional chaining을 적용하지 않고, 함수 입구에서 URL을 한 번 정규화해 이후 분기 전체의 null 안전성을 보장한다.

## Current Status - 출처 없는 카드의 Side Panel 렌더링 오류 수정

- `npm.cmd run check`: 문법 검사와 전체 85개 테스트 통과.
- Chrome UI harness에서 출처가 없는 Application Fee 카드를 포함한 전체 결과가 렌더링되는 것을 확인했다.
- 브라우저 경고·오류 로그 0개, 상세 펼침 상호작용 정상 동작을 확인했다.
- 실제 설치된 확장에는 재로딩 후 반영된다.

## Next Steps - 출처 없는 카드의 Side Panel 렌더링 오류 수정

- Chrome 확장 관리 화면에서 최신 로컬 확장을 다시 로드하고 실제 학교 페이지에서 Side Panel을 다시 연다.
- 이전 오류 기록은 Chrome 확장 오류 목록에 남아 있을 수 있으므로 새 실행에서 같은 오류가 추가되는지를 기준으로 확인한다.

## Work Summary - UCL South Korea 결과 로딩 대기 보강

UCL에서 국가 선택은 실제로 `South Korea`로 바뀌었지만, 첫 자동 분석이 동등학력 결과 DOM이 생성되기 전에 끝나 Korean Equivalent가 선택 필요 상태로 남는 회귀를 확인했다. 동시에 빈 영어 세부 URL이 Korean Equivalent 카드의 `등급 세부 기준` 링크로 잘못 표시되는 문제도 발견했다.

## Work Completed - UCL South Korea 결과 로딩 대기 보강

- 국가 선택 후 고정 350ms를 기다리던 방식을 제거하고, South Korea 동등학력 본문이 실제로 생성됐는지 최대 4초 동안 100ms 간격으로 확인하도록 변경했다.
- 이미 South Korea가 선택됐지만 결과가 아직 로딩 중인 경우에도 같은 대기 로직을 적용했다.
- 동적으로 추가되는 국가별 제목을 읽을 수 있도록 `collectSection`이 호출 시점의 최신 heading 목록을 사용하도록 변경했다.
- 테스트에서 실제 UCL option 값인 `KR`을 사용하고, change 이벤트 30ms 뒤 한국 GPA 본문이 생성되는 비동기 DOM을 구성해 첫 분석에서 값을 얻는지 확인했다.
- 원문 URL과 등급 세부 URL이 빈 문자열이면 액션 링크를 생성하지 않도록 Side Panel 조건을 보강했다.

## Decisions - UCL South Korea 결과 로딩 대기 보강

- 페이지 반응 시간을 임의의 한 번 대기로 추정하지 않고, 필요한 결과의 존재를 성공 조건으로 삼는다. 결과가 즉시 있으면 지연 없이 진행하고, 느린 경우에만 필요한 만큼 기다린다.
- 메모 입력에서는 빈 출처 URL을 허용하지만 결과 카드의 액션은 실제 URL이 있을 때만 표시해야 하므로, URL 유효성 검사와 링크 표시 조건을 분리했다.

## Current Status - UCL South Korea 결과 로딩 대기 보강

- `npm.cmd run check`: 문법 검사와 전체 85개 테스트 통과.
- 실제 UCL Computational Finance 페이지에서 South Korea option 값 `KR`, `Equivalent qualifications for South Korea`, GPA `3.5/4.5 or 3.3/4.3` 본문이 현재 DOM에 존재함을 확인했다.
- 실제 페이지의 브라우저 경고·오류 로그는 0개였다.
- 설치된 확장 프로그램에서 첫 분석 결과를 확인하려면 최신 로컬 확장을 다시 로드해야 한다.

## Next Steps - UCL South Korea 결과 로딩 대기 보강

- Chrome 확장 관리 화면에서 최신 로컬 확장을 다시 로드하고 UCL 페이지를 새로고침한다.
- Side Panel의 첫 자동 분석만으로 Korean Equivalent에 GPA가 표시되고, 해당 카드에 `등급 세부 기준` 링크가 나타나지 않는지 확인한다.

## Work Summary - Manchester 통합 과정 페이지 지원

Alliance MBS의 섹션별 페이지와 별개로 운영되는 Manchester 본교의 한 페이지형 과정 구조를 조사했다. Mechanical Engineering Design MSc에서 누락되던 한국 동등학력, staged admission, 지원 서류 조건을 수집하고, CAS 보증금이 학비로 섞이던 문제를 수정했다.

## Work Completed - Manchester 통합 과정 페이지 지원

- Manchester 본교 과정 URL을 기존 Alliance MBS 전용 reader와 분리해 generic reader 기반의 두 번째 공식 템플릿으로 등록했다.
- 공식 South Korea 입학 기준 페이지의 `PG master’s courses` 문장형 GPA를 읽어 과정의 Upper Second 기준과 `GPA 3.5/4.5 or GPA 3.3/4.3`으로 연결했다.
- 쉼표가 없는 `£2500`을 `£250`으로 잘라 읽던 금액 정규식을 수정하고, CAS deposit 문맥은 tuition 후보에서 제외했다.
- `div.text`에 묶인 Manchester 지원 서류 본문을 보조 수집 범위로 추가해 References와 personal statements의 불필요 상태를 인식하게 했다.
- `A CV if you graduated more than three years ago`를 조건부 필수 CV로 구조화했다.
- staged admission 안내는 종류별 한 건으로 중복 제거하고, 다음 단계로 이월될 수 있다는 중간 날짜를 일반 지원 마감일로 확정하지 않도록 했다.
- Manchester 본교 구조를 재현한 reader-parser 통합 테스트와 사이트 등록 회귀 테스트를 추가했다.

## Decisions - Manchester 통합 과정 페이지 지원

- 같은 Manchester라도 Alliance MBS와 본교의 URL·DOM 계약이 다르므로 하나의 학교 전용 reader에 합치지 않고, URL 패턴별 템플릿 설정을 분리했다.
- 보증금 숫자를 특정 금액으로 차단하지 않고 `deposit` 의미 문맥으로 제외해 향후 금액 변경에도 대응하도록 했다.
- Manchester의 한국 기준은 표가 아니라 한 문단이며 과정 기본 조건이 Upper Second이므로, 사이트 설정에 해당 기본 등급을 명시하고 GPA 문장만 공통 파서가 구조화하도록 했다.
- staged admission에는 여러 날짜가 있어도 단일 마감일로 임의 확정하지 않고, 단계별 일정을 원문에서 확인하는 상태를 유지한다.

## Current Status - Manchester 통합 과정 페이지 지원

- 실제 과정 페이지에서 국제학생 학비 `£38,400`, 별도 CAS deposit `£2500`, staged admission 문구, 조건부 CV, References와 personal statements 불필요 문구의 DOM 위치를 확인했다.
- 실제 South Korea 공식 페이지에서 `GPA 3.5/4.5`와 `GPA 3.3/4.3` 기준을 확인했다.
- `npm.cmd run check`: 문법 검사와 전체 89개 테스트 통과.
- `git diff --check`: 공백 오류 없음. 로컬 변경은 아직 커밋하거나 배포하지 않았다.

## Next Steps - Manchester 통합 과정 페이지 지원

- Chrome 확장 관리 화면에서 로컬 확장을 다시 로드하고 Manchester 본교 과정 페이지를 새로고침한다.
- Side Panel에서 Korean Equivalent, Tuition Fee, University Application Deadline, Reference, SOP Guideline, CV 여섯 항목을 실제 설치본으로 최종 확인한다.

## Work Summary - 범용 Entry Requirements v2

KCL처럼 입학 기준이 한 페이지에 단정하게 펼쳐진 구조만 기준으로 삼았을 때 발생한 과다 수집 문제를 해결하기 위해, 학교별 예외 수집기를 늘리지 않고 여러 DOM 범위 후보 중 학력 조건에 가장 가까운 내용을 선택하는 범용 Entry Requirements 수집 방식을 구현했다.

## Work Completed - 범용 Entry Requirements v2

- 기존의 첫 번째 `Entry Requirements` 제목 다음 내용을 그대로 가져오는 단일 경로에 더해, 동일 Entry 컨테이너 안의 직접 블록을 별도 후보로 생성하도록 했다.
- 후보마다 학위 등급·학위 명칭·동등 자격 문맥에는 가점을 주고, English language·CV·Admissions Statement·지원 방법·지원 서류·다수 국가명·지나치게 긴 본문에는 감점을 주어 가장 적합한 후보를 선택하게 했다.
- 드롭다운의 국가 목록은 Entry 본문 후보에서 제거하고, English language·국제 자격·지원 서류 등의 다음 섹션을 만나면 수집을 중단하도록 경계를 보강했다.
- Manchester, Edinburgh, Leeds, Birmingham, Nottingham, Bristol, York, Loughborough, Southampton, Exeter의 실제 실패 구조를 대표하는 10개 DOM 회귀 사례를 추가했다.

## Decisions - 범용 Entry Requirements v2

- 10개 학교 각각에 전용 selector를 추가하지 않고 `후보 생성 → 공통 점수 평가 → 최적 후보 선택` 구조를 사용했다. 앞으로 새로운 실패가 생겨도 학교 이름이 아니라 실패 계열과 점수 규칙을 보완하기 위한 결정이다.
- 900자 이상이라는 이유만으로 내용을 자르지 않고, 학력 조건 신호와 오염 신호를 함께 평가해 비교적 긴 정상 입학 기준도 보존하도록 했다.
- 기존 수집 후보와 새 컨테이너 후보의 점수가 같으면 기존 후보를 유지해 이미 정상 작동하던 KCL·QMUL 계열의 동작 변경을 최소화했다.

## Current Status - 범용 Entry Requirements v2

- `node --test tests/generic-reader.test.js`: 집중 테스트 13개 통과.
- `npm.cmd run check`: 문법 검사와 전체 90개 테스트 통과.
- 10개 실제 학교 페이지의 DOM 실패 형태와 수정 전 후보를 확인한 뒤 해당 구조를 회귀 테스트로 고정했다.
- 수정 코드를 실제 10개 페이지 안에서 다시 실행하는 자동 검증은 Chrome 보안 정책에서 차단되어 우회하지 않았다. 설치된 확장 프로그램을 다시 로드한 뒤 실제 Side Panel 확인은 남아 있다.
- 로컬 변경은 아직 커밋하거나 배포하지 않았다.

## Next Steps - 범용 Entry Requirements v2

- Chrome 확장 관리 화면에서 로컬 확장을 다시 로드하고 우선 Bristol, Loughborough, Birmingham에서 Entry Requirements 과다 수집이 사라졌는지 확인한다.
- 실제 설치본에서 남는 실패가 있으면 학교별 코드를 바로 추가하지 않고, 해당 DOM이 기존 10개 실패 계열 중 어디에 속하는지 먼저 판별한다.
