# 2026-07-30 Admission Extension Development Log

## Work Summary

입학요강 정보 추출 Chrome Extension의 실제 개발을 시작하기 전에 현재 위치와 단계별 완료 조건을 확인할 수 있도록 Roadmap과 Phase 0 계획서를 작성했다. 프로젝트 초기에 범위가 커져 길을 잃지 않도록 첫 구현을 Chrome 확장 기본 골격으로 제한했다.

## Work Completed

- 프로젝트 전체 단계를 Phase 0부터 Phase 5까지 구분한 `ROADMAP.md`를 작성했다.
- 현재 상태를 `Phase 0 — 다음 작업`으로 명시했다.
- KCL, SOAS, QMUL을 각각 다른 페이지 구조를 검증하는 표본으로 배치했다.
- Phase 0의 사용자 흐름, 화면 구성, 권한, 데이터 구조, 저장 범위, 접근성, 오류 처리, 테스트, 완료 조건을 문서화했다.
- Chrome Side Panel을 확장 아이콘 클릭 이벤트에서 여는 구조와 최소 Chrome 116 기준을 확정했다.
- 아직 구현하지 않은 추출·복사·메모·AI 기능을 Phase 0 제외 범위로 분리했다.

## Decisions

- Roadmap은 기능 목록보다 현재 위치와 완료 여부를 빠르게 확인하는 문서로 사용한다.
- Phase 0에서는 실제 대학 페이지 내용을 추출하지 않고 Side Panel과 고정 UI, 앱 전체 기준, 로컬 저장 구조만 검증한다.
- 첫 실제 추출은 Phase 1에서 KCL 한 과정 완주 방식으로 구현한다.
- 권한은 단계별로 필요한 만큼만 추가하고 Phase 0에서는 `<all_urls>`나 대학 사이트 전체 권한을 요청하지 않는다.
- 앱 전체 기준은 자동으로 다음 연도로 넘어가지 않고 J님이 직접 변경한 값을 유지한다.
- Word, Notion, 로그인된 지원서 페이지, AI는 현재 구현 범위에서 제외한다.

## Current Status

- Roadmap 작성 완료
- Phase 0 계획서 작성 완료
- 확장 프로그램 코드 구현 전
- 자동 테스트와 Chrome 동작 검증은 아직 수행하지 않음
- 현재 폴더는 아직 Git 저장소가 아님

## Next Steps

- Phase 0 파일 구조와 Manifest V3 확장 골격 구현
- 확장 아이콘 클릭과 Side Panel 연결
- 현재 페이지 제목·URL 표시
- 앱 전체 기준값 저장과 변경 UI 구현
- 11개 항목 카드 렌더링
- 자동 검사와 실제 Chrome 설치 검증
- 검증 완료 후 Roadmap의 Phase 0 상태를 `완료`로 갱신

## Work Summary - Phase 0 구현

Phase 0 계획서에 정의한 Chrome Extension 기본 골격을 구현했다. 실제 입학요강 추출 기능은 포함하지 않고, 확장 아이콘과 Side Panel 연결, 앱 전체 기준값, 현재 페이지 정보, 11개 고정 항목, 로컬 저장 및 예외 안내가 다음 단계의 기반이 되도록 구성했다.

## Work Completed - Phase 0 구현

- Manifest V3, Chrome 116 이상, ES Module 기반의 빌드 없는 확장 프로그램 구조를 만들었다.
- `activeTab`, `sidePanel`, `storage` 세 권한만 요청하고 팝업이나 고정 호스트 권한은 추가하지 않았다.
- 확장 아이콘 클릭 이벤트 안에서 현재 탭에 연결된 Side Panel을 여는 서비스 워커를 구현했다.
- 현재 탭의 제목과 복사 가능한 전체 URL을 표시하고, 다시 확인하는 동작과 `chrome://` 등 제한 페이지 안내를 구현했다.
- 학년도, 입학 월·연도, Study mode, Fee status를 변경하는 대화상자와 `chrome.storage.local` 저장 구조를 구현했다.
- 9월이 아닌 입학 회차의 주의 안내와 `9월 기준으로 되돌리기` 동작을 구현했다.
- 정해진 11개 항목을 동일한 순서와 `분석 전` 상태로 렌더링하고, Phase 1 전에는 실제 분석을 하지 않는다는 안내를 표시했다.
- 키보드로 사용할 수 있는 네이티브 컨트롤, 가시적인 포커스, 상태 아이콘과 문구, 충분한 클릭 영역을 적용했다.
- 학년도·입학 월 검증, 첫 실행 초기화, 손상 저장값 복구, 저장 실패 처리, 필드 순서, Manifest 최소 권한을 검사하는 Node 테스트 10개를 작성했다.
- `npm.cmd run check`로 JavaScript 문법 검사와 테스트 10개 통과를 확인했다.
- Chrome API 응답을 모사한 로컬 브라우저 검증에서 현재 페이지 표시, 11개 카드 순서, 기준 변경과 새로고침 후 유지, 비9월 경고와 9월 복귀, 제한 페이지 안내, 콘솔 오류 없음을 확인했다.
- 시각 검증 중 `hidden` 속성이 공통 알림 CSS에 덮여 9월에도 주의 상자가 보이는 문제를 발견하고 수정했다.

## Decisions - Phase 0 구현

- 현재 페이지 정보는 방문 기록으로 저장하지 않고 Side Panel이 열리거나 다시 확인할 때 활성 탭에서 읽는다.
- 저장 실패 시 화면의 기존 기준값을 바꾸지 않고 재시도 안내를 표시한다.
- 저장된 기준이 손상된 경우에만 계획서 기본값으로 복구하며, UI 버전 보정 저장이 실패해도 유효한 기존 기준은 유지한다.
- Phase 1 범위인 본문 분석, `scripting` 권한, 항목별 복사와 결과 수정은 구현하지 않았다.
- 자동화 도구의 보안 정책상 `chrome://extensions` 관리 화면을 조작할 수 없었으므로 설치를 우회하지 않았다. 실제 Chrome 설치와 아이콘 클릭 검증이 끝나기 전에는 Phase 0를 완료로 표시하지 않는다.

## Current Status - Phase 0 구현

- Phase 0 코드 구현 완료
- JavaScript 문법 검사 통과
- Node 자동 테스트 10개 통과
- 로컬 Side Panel 화면·상호작용 검증 통과
- 실제 Chrome 압축 해제 설치 및 확장 아이콘 클릭 검증 미수행
- Roadmap의 Phase 0 상태는 `진행 중`

## Next Steps - Phase 0 구현

- `chrome://extensions`에서 이 프로젝트 폴더를 압축 해제된 확장 프로그램으로 불러온다.
- KCL 과정 페이지에서 확장 아이콘 클릭, Side Panel 열림, 현재 페이지 제목·URL을 확인한다.
- 기준 변경 후 패널을 닫고 다시 열어 저장 유지 여부를 확인한다.
- `chrome://extensions` 탭에서 제한 페이지 안내가 보이는지 확인한다.
- 처리되지 않은 콘솔 오류가 없는지 확인한 뒤 Roadmap의 Phase 0를 `완료`로 변경한다.

## Work Summary - Phase 0 완료 및 Phase 1 계획

J님의 실제 Chrome 확인 결과를 반영해 Phase 0를 완료로 전환하고, 첫 실제 추출 단계인 Phase 1의 구현 계획을 작성했다. Phase 1 계획은 모든 카드를 억지로 채우는 방식이 아니라 KCL Nutrition MSc 한 과정에서 찾은 값, 사용자 행동이 필요한 값, 공개되지 않은 값, 연결 실패를 구분하는 흐름으로 정리했다.

## Work Completed - Phase 0 완료 및 Phase 1 계획

- 실제 Chrome에서 압축 해제 설치, 확장 아이콘 클릭, Side Panel 열림, 현재 페이지 제목·URL 표시, 기준 변경 후 저장 유지를 확인한 결과를 반영했다.
- 자동 테스트와 로컬 예외 검증을 합쳐 Phase 0를 `완료`로 변경했다.
- Roadmap에서 Phase 1을 유일한 `진행 중` 단계로 변경했다.
- KCL Nutrition MSc Requirements와 Fees 공개 페이지의 현재 내용을 조사했다.
- 11개 항목별 출처, 추출 규칙, 상태, 기준 불일치 처리, 출처 표시, 복사 규칙을 정의했다.
- South Korea 국가 선택 자동화는 제외하고 미선택 상태를 `사용자 확인 필요`로 처리하도록 정했다.
- 공개 페이지에 정확한 Application Deadline이 없으면 날짜를 추측하지 않고 `찾지 못함`으로 처리하도록 정했다.
- Requirements DOM에서 실제 Fees 링크를 발견하기 전에는 `/fees` URL을 생성하지 않는 규칙을 정했다.
- `activeTab`과 `scripting`을 이용한 현재 탭 분석과 same-origin Fees 요청을 기본안으로 정하고, 고정 host permission 추가는 실제 실패 근거가 있을 때만 검토하도록 했다.
- DOM 읽기와 순수 파서를 분리하고 공개 정보의 최소 JSON snapshot으로 테스트하는 구조를 계획했다.
- 마지막 분석 하나와 제한된 이벤트만 로컬에 저장하고 전체 HTML과 페이지 본문은 저장하지 않는 범위를 정했다.

## Decisions - Phase 0 완료 및 Phase 1 계획

- J님이 확인한 실제 Chrome 핵심 흐름과 이미 통과한 자동·로컬 예외 검증을 Phase 0 완료 근거로 사용했다.
- KCL 한 과정에서 일부 항목이 공개되지 않거나 추가 행동이 필요해도 상태와 이유가 정확하면 정상 결과로 본다.
- Korean Academic Requirements는 South Korea가 직접 선택된 뒤에만 확정하고 다른 국가 값을 대신 사용하지 않는다.
- English Requirements는 현재 페이지의 Band B까지만 다루고 상세 IELTS 점수를 다른 페이지에서 자동 수집하지 않는다.
- SOP Guideline 카드는 KCL 원문의 Personal Statement를 사용하되 출처 명칭을 숨기지 않는다.
- Tuition Fee는 학년도, Study mode, Fee status가 모두 현재 앱 기준과 일치할 때만 확정한다.
- 추출 상태와 복사 상태를 분리해 복사 후에도 값의 출처와 추출 결과를 유지한다.
- 실제 DOM에서 same-origin Fees 요청이 실패하기 전에는 KCL host permission을 추가하지 않는다.

## Current Status - Phase 0 완료 및 Phase 1 계획

- Phase 0 완료
- Phase 1 계획서 작성 완료
- Phase 1 상태는 `진행 중`
- Phase 1 코드 구현 전
- 다음 구현 진입점은 P1-0 실제 KCL DOM preflight
- 현재 폴더는 아직 Git 저장소가 아님

## Next Steps - Phase 0 완료 및 Phase 1 계획

- 실제 Chrome의 KCL Requirements DOM에서 과정명, heading, supporting documents 행, Fees 링크를 확인한다.
- South Korea 선택 전후 DOM 차이를 확인한다.
- 공개 정보만 담은 최소 snapshot fixture를 작성한다.
- fixture가 확정된 뒤 추출 상태와 순수 KCL parser부터 구현한다.

## Work Summary - Phase 1 구현

KCL Nutrition MSc 한 과정을 대상으로 Requirements와 실제 Fees 링크의 공개 정보를 11개 카드로 분류하는 Phase 1 코드를 구현했다. 초기 조사와 달리 동적 렌더링이 끝난 실제 DOM에는 fee status별 지원 마감일이 존재했으므로 계획과 추출 규칙을 현재 페이지 사실에 맞게 수정했다.

## Work Completed - Phase 1 구현

- 실제 KCL Requirements DOM에서 heading 구조, 과정 메뉴의 Fees 링크, South Korea 선택 전후 값, 지원 서류 표, Application Fee, English Band B, fee status별 지원 마감일을 확인했다.
- Fees 페이지에서 2026/27 Full-time UK `£18,150`, International `£38,300`의 실제 heading과 금액 구조를 확인했다.
- `found`, `action_required`, `not_found`, `multiple_candidates`, `source_error` 등을 분리한 추출 상태와 별도의 복사 상태를 구현했다.
- Requirements와 Fees 최소 JSON snapshot, 실제 같은 과정 Fees 링크 검증, 11개 항목 순수 parser를 구현했다.
- 현재 DOM의 실제 Fees 링크만 사용하고, 동적 콘텐츠가 준비될 때까지 제한적으로 기다린 뒤 same-origin Fees 요청을 수행하는 페이지 리더를 구현했다.
- 앱 기준의 Academic cycle, Study mode, Fee status가 모두 맞는 Tuition Fee만 선택하고, 지원 마감일은 Fee status와 입학 연도가 맞을 때만 확정하도록 했다.
- South Korea 미선택 또는 다른 국가 선택 상태에서는 학력 요건을 추정하지 않고 다음 행동을 표시하도록 했다.
- Side Panel에 분석 진행·완료 요약, 상태별 카드, 값과 설명, 다음 행동, 출처 원문, 항목별 복사 UI를 구현했다.
- 마지막 분석 결과, stale 상태, 최대 100개의 분석·복사 이벤트를 `chrome.storage.local`에 저장하고 전체 HTML은 저장하지 않도록 했다.
- Manifest 버전을 `0.2.0`으로 올리고 `scripting` 권한만 추가했으며, 고정 host permission은 추가하지 않았다.
- 문법 검사와 parser·저장·Manifest 테스트를 포함한 자동 검사 20개 통과를 확인했다.
- 로컬 Side Panel fixture에서 11/11 결과 렌더링, 복사 후 `복사됨`, 기준 변경 후 stale 안내, Home 재분석 시 UK 학비 `£18,150`와 마감일 `25 August 2026` 전환을 확인했다.

## Decisions - Phase 1 구현

- Requirements에서 과정 헤더가 동적 상태에 따라 사라질 수 있어, 같은 DOM에서 검증한 실제 Fees 링크의 과정 헤더를 보조 근거로 사용한다.
- Application Deadline은 더 이상 공개되지 않은 값으로 취급하지 않고, International은 Overseas 행, Home은 Home 행을 선택한다. 입학 연도가 다르면 자동 확정하지 않는다.
- Fees 요청 실패는 Tuition Fee의 `source_error`로 격리해 Requirements에서 이미 확인한 다른 결과를 지우지 않는다.
- 기준 변경 시 이전 결과를 즉시 삭제하지 않고 stale로 표시해 비교 가능성을 남긴다.
- 실제 확장 환경에서 same-origin Fees 요청이 실패한다는 근거가 생기기 전에는 KCL host permission을 추가하지 않는다.

## Current Status - Phase 1 구현

- P1-0부터 P1-4 코드 구현 완료
- 실제 KCL DOM preflight 완료
- JavaScript 문법 검사 통과
- Node 자동 테스트 20개 통과
- Phase 1 상태는 실제 Chrome 수동 검증이 남아 있어 `진행 중`
- 현재 폴더는 아직 Git 저장소가 아님

## Next Steps - Phase 1 구현

- `chrome://extensions`에서 Admission Checklist 확장의 새로고침 버튼을 누른다.
- KCL Requirements 페이지에서 확장 아이콘을 눌러 자동 분석과 11개 카드 결과를 확인한다.
- South Korea 미선택 안내와 선택 후 Korean Academic Requirements 갱신을 확인한다.
- International 학비 `£38,300`, 지원비 `£85`, 마감일 `25 July 2026`이 표시되는지 확인한다.
- 항목 복사 후 `복사됨`, 패널 재실행 후 마지막 분석 유지, 기준 변경 후 stale 안내를 확인한다.
- Side Panel과 서비스 워커의 처리되지 않은 콘솔 오류가 없는지 확인한 뒤 Phase 1을 완료로 변경한다.

## Work Summary - Phase 1 결과 간소화

실제 사용 중 확인된 스크롤 길이와 복사 중복을 줄이기 위해 카드별 표시값과 복사값을 다시 정의했다. 정보 자체는 유지하되 학교·과정명에는 불필요한 행동을 없애고, 영어와 비용은 바로 옮겨 쓸 핵심 조건만 보이도록 정리했다.

## Work Completed - Phase 1 결과 간소화

- University와 Course 카드에서 출처 확인과 복사 버튼을 제거했다.
- English Requirements가 IELTS 점수 문장, 영문·숫자 Band, 별도 페이지 링크의 세 형태를 구분하도록 확장했다.
- 영어 카드에는 점수 또는 Band 한 줄만 표시하고 일반 영어 능력 안내 문단을 제거했다.
- Tuition Fee와 Application Fee는 통화기호가 있으면 통화기호와 금액만, 통화기호가 없으면 금액 숫자만 표시하도록 변경했다.
- 모든 카드의 기본 복사 규칙을 `값 + 설명` 자동 결합에서 항목별 대표 본문 복사로 변경했다.
- Deadline은 화면에 날짜를 한 번만 표시하고, 복사할 때는 페이지의 fee status 포함 원문 한 줄만 사용하도록 변경했다.
- Reference, SOP, CV는 상태 라벨보다 실제 안내 본문을 복사하도록 정리했다.
- 이전 저장 결과가 새 표시 규칙을 가리지 않도록 분석 schema를 2로 올리고 확장 버전을 `0.2.1`로 변경했다.
- IELTS, Band, 링크 전용, 통화기호 없는 비용, Deadline 중복 방지 테스트를 추가했다.
- 자동 검사 24개 통과와 로컬 Side Panel의 간결한 카드 렌더링·복사 상태를 확인했다.

## Decisions - Phase 1 결과 간소화

- 출처는 검증과 문제 해결에 필요한 카드에만 제공하고, 식별값인 University와 Course에는 노출하지 않는다.
- 영어 조건은 앱에 이미 보유한 별도 데이터와 중복되지 않도록 페이지가 직접 밝힌 조건 형태만 기록한다.
- 통화기호가 없는 학교의 비용도 실패로 처리하지 않되, 학년도처럼 금액이 아닌 숫자를 비용으로 선택하지 않도록 한다.
- 복사값은 화면 구성 요소를 기계적으로 합치지 않고 업무에 실제로 붙여넣을 텍스트를 항목별로 지정한다.

## Current Status - Phase 1 결과 간소화

- 요청한 결과 간소화와 복사 중복 수정 완료
- JavaScript 문법 검사 통과
- Node 자동 테스트 24개 통과
- 로컬 Side Panel 렌더링과 Deadline 복사 상태 검증 완료
- 새 코드의 실제 Chrome 확장 새로고침 확인은 남아 있음

## Next Steps - Phase 1 결과 간소화

- Chrome 확장을 새로고침하고 KCL 페이지에서 간결해진 카드 결과를 확인한다.
- South Korea 자동 선택은 기술적으로 가능하므로 J님이 원하면 별도 변경으로 구현한다.
- 자동 선택 구현 시 국가명 별칭을 정확히 제한하고 선택 후 실제 학력 요건이 렌더링됐는지 재검증한다.

## Work Summary - Phase 2 계획

KCL 한 과정에서 만든 추출 구조를 SOAS와 QMUL에 확장하기 위한 Phase 2 계획서를 작성했다. 단순히 학교별 selector를 추가하는 단계가 되지 않도록 학교별 DOM reader와 공통 후보 parser의 경계를 정하고, J님의 사용 방식에 맞춰 South Korea 자동 확인을 Phase 2의 공통 resolver 작업으로 배치했다.

## Work Completed - Phase 2 계획

- SOAS MSc Global Development와 QMUL Corporate Finance MSc의 현재 공개 페이지를 조사했다.
- SOAS의 긴 단일 페이지, 전체 국가 목록, 학년도 없는 fee, 별도 English 링크, optional references 구조를 기록했다.
- SOAS 과정 페이지의 장학금 deadline을 University Application Deadline으로 오인하지 않는 규칙을 정했다.
- QMUL의 September 2026·2027 입학 블록, 2027 fee 발표 예정 상태, deadline 미정 상태, supporting documents 구조를 기록했다.
- QMUL 과정의 2:2 요구 등급과 South Korea의 2:2 동등 GPA 행을 연결하는 규칙을 정했다.
- 학교별 코드는 DOM을 공통 snapshot으로 정규화하는 adapter에만 두고, 기준 선택과 11개 결과 생성은 공통 parser가 담당하도록 설계했다.
- South Korea가 이미 DOM에 있으면 조작하지 않고 사용하며, 없을 때만 exact alias로 드롭다운을 자동 선택하고 결과 본문을 재검증하도록 계획했다.
- South Korea, Republic of Korea 등의 허용 별칭을 명시하고 North Korea나 모호한 fuzzy match를 금지했다.
- 학년도 없는 SOAS fee, QMUL의 `Fees will be announced`, `To be confirmed`를 이전 연도 값으로 대체하지 않는 상태 규칙을 정했다.
- 공통 snapshot schema 3, reason code, 저장 범위, 권한, 자동 테스트, 실제 Chrome 검증, P2-0부터 P2-7까지의 구현 순서를 문서화했다.
- Roadmap에서 Phase 2를 `다음 작업`으로 변경하고 상세 계획서 링크를 연결했다.
- Phase 5 후보였던 South Korea 자동 선택을 Phase 2의 공통 처리 범위로 이동했다.

## Decisions - Phase 2 계획

- Phase 1 최신 코드의 실제 Chrome 확인이 남아 있으므로 Phase 1은 `진행 중`으로 유지하고 Phase 2는 `다음 작업`으로 표시한다.
- 학교별 DOM 차이를 완전히 없애려 하지 않고, DOM adapter 밖으로 학교명 조건이 퍼지지 않게 하는 것을 일반화 기준으로 삼는다.
- South Korea 자동 선택은 여러 학교의 표기와 control 구조를 비교할 수 있는 Phase 2가 적절한 시점으로 판단했다.
- 국가 선택은 exact alias가 하나일 때만 수행하고, 선택 후 국가명과 학력 본문이 모두 확인돼야 성공으로 기록한다.
- SOAS처럼 국가별 내용이 이미 DOM에 모두 있으면 드롭다운을 조작하지 않고 South Korea 블록 경계를 직접 추출한다.
- 미발표·미정·학년도 미표시는 단순 빈칸과 구분하고, 확정 전에는 복사할 수 없게 한다.
- 현재 권한인 `activeTab`, `scripting`, `sidePanel`, `storage`를 유지하고 고정 host permission은 실제 실패 근거가 있을 때만 검토한다.

## Current Status - Phase 2 계획

- Phase 2 계획서 작성 완료
- Roadmap 연결 완료
- Phase 1 최신 코드 실제 Chrome 확인 대기
- Phase 2 구현 전
- 다음 구현 진입점은 P2-0 SOAS·QMUL 실제 DOM preflight
- 현재 폴더는 아직 Git 저장소가 아님

## Next Steps - Phase 2 계획

- Chrome에서 확장 버전 `0.2.1`을 새로고침하고 Phase 1 간결화 결과를 확인한다.
- Phase 1을 완료로 변경한 뒤 Phase 2를 `진행 중`으로 전환한다.
- SOAS와 QMUL의 실제 DOM에서 heading, 국가 control, 입학 회차 블록 경계를 확인한다.
- 공개 정보만 담은 SOAS·QMUL 최소 fixture를 작성한다.
- site adapter contract와 공통 snapshot schema 3부터 구현한다.

## Work Summary - Phase 2 구현

SOAS와 QMUL의 실제 공개 페이지를 Chrome에서 직접 확인한 뒤, 학교별 DOM을 같은 11개 결과로 바꾸는 Phase 2 핵심 흐름을 구현했다. J님이 자주 확인하는 12개 추가 과정 링크는 곧바로 지원 대상으로 넓히지 않고, 이후 adapter 우선순위와 구조 회귀 검증에 사용할 holdout 세트로 계획서에 기록했다.

## Work Completed - Phase 2 구현

- SOAS와 QMUL의 heading, key information, 국가별 학력, 입학 회차, 학비·마감일, 제출 서류 DOM 경계를 실제 브라우저에서 확인했다.
- KCL·SOAS·QMUL URL을 판정하는 site registry와 공통 Course Snapshot schema 3을 추가했다.
- SOAS 긴 페이지에서 South Korea 2:ii GPA 블록, 별도 English 링크, 학년도 없는 국제 학비, optional reference, supporting statement 언급을 추출하는 adapter를 구현했다.
- QMUL에서 일반 2:2 조건과 South Korea 2:2 GPA 행을 연결하고, 2026·2027 intake의 학비와 deadline을 분리하는 adapter를 구현했다.
- QMUL 2027의 `Fees will be announced`와 `To be confirmed`를 이전 연도 값으로 대체하지 않고 `action_required`로 유지했다.
- 공통 parser에서 기준 일치 후보 선택, 금액 간소화, 미발표·학년도 없음 reason code, 확정 전 복사 금지를 구현했다.
- South Korea exact alias resolver를 추가하고 `Korea`, `North Korea`와 복수 후보를 자동 선택하지 않도록 제한했다.
- KCL custom dropdown은 South Korea 옵션이 정확히 하나일 때만 선택하고 실제 South Korea 본문이 나타났는지 다시 확인하도록 보강했다.
- Side Panel을 세 학교 reader/parser에 연결하고 저장 schema와 UI 버전을 3으로 올렸다.
- 확장 버전을 `0.3.0`으로 변경했으며 기존 최소 권한 네 개를 유지했다.
- SOAS·QMUL fixture, 공통 parser, site registry, country resolver, schema 3 저장 테스트를 추가했다.
- 실제 SOAS·QMUL 페이지에서 새 adapter와 parser를 직접 실행해 11개 결과를 확인했다.

## Decisions - Phase 2 구현

- SOAS처럼 국가별 본문이 이미 DOM에 있으면 dropdown을 건드리지 않고 정확한 South Korea 블록을 바로 사용한다.
- QMUL의 숨겨진 `KOR` 블록은 화면 선택 상태와 무관하게 원문 근거로 사용할 수 있으므로 조작보다 직접 추출을 우선한다.
- SOAS 학비는 금액 `£25,320`을 보여주되 적용 학년을 확인할 수 없으므로 확정·복사를 허용하지 않는다.
- SOAS 과정 페이지의 장학금 마감일은 지원 마감일로 사용하지 않는다.
- 학교별 DOM selector는 content adapter 안에 두고, 기준 비교와 상태·복사 판단은 공통 parser가 담당한다.
- 추가 12개 링크는 학교 개수보다 구조 유형을 기준으로 분류하며, 인기 5개교를 다음 지원 후보로 우선 검증한다.
- Henley/Reading의 별도 도메인, Bath의 January/year URL, Bristol의 PDF 중심 정보는 별도 구조 유형으로 보존한다.

## Current Status - Phase 2 구현

- P2-0부터 P2-6까지 핵심 코드 구현 완료
- 실제 SOAS·QMUL DOM adapter 실행과 공통 parser 결과 확인 완료
- KCL South Korea 선택 후 본문 추출 회귀 확인 완료
- JavaScript 문법 검사와 자동 테스트 30개 전체 통과
- 로컬 UI harness에서 KCL 11/11, QMUL 10/11, SOAS 6/11 및 상태·복사 버튼 렌더링 확인
- 실제 설치된 확장 버전 `0.3.0`의 Side Panel 수동 검증은 남아 있음
- 현재 폴더는 아직 Git 저장소가 아님

## Next Steps - Phase 2 구현

- Chrome 확장 관리 화면에서 `0.3.0`을 새로고침한다.
- SOAS와 QMUL 과정 페이지에서 확장 아이콘을 눌러 실제 Side Panel 분석 결과와 복사 상태를 확인한다.
- KCL에서 국가 미선택 상태로 다시 분석해 South Korea 자동 선택이 실제 확장 컨텍스트에서도 동작하는지 확인한다.
- 수동 검증이 끝나면 Phase 2 완료 여부와 다음 인기 학교 adapter 우선순위를 확정한다.

## Work Summary - 0.3.1 실사용 피드백 수정

J님의 `0.3.0` 실제 Chrome 확인에서 발견된 KCL 과정명 누락과 QMUL 과정 요구 등급·한국 동등 성적 불일치를 수정했다. SOAS는 문제 없음이 확인되어 기존 adapter를 유지했다.

## Work Completed - 0.3.1 실사용 피드백 수정

- 현재 KCL Requirements 페이지에서 과정명이 과정 `h1`이 아니라 `og:title`과 문서 제목에 존재하는 구조를 실제 DOM으로 확인했다.
- KCL 과정명은 화면 전용 요소를 먼저 사용하고, 없으면 metadata의 `Entry Requirements` 접미사를 제거한 뒤 URL의 학위 접미사만 결합하도록 보강했다.
- KCL Nutrition 페이지에서 `Nutrition MSc`가 추출되는 것을 실제 공개 DOM으로 확인했다.
- QMUL adapter가 South Korea 본문을 `UK 2:2 degree`부터 잘라내던 고정 규칙을 제거했다.
- QMUL South Korea의 1st, 2:1, 2:2, 3rd, ordinary/pass 행을 후보 배열로 보존하도록 변경했다.
- 과정 조건의 `1st/first-class/1:1`, `2:1/upper second`, `2:2/lower second`, `3rd/third-class`, `ordinary/pass`를 다섯 등급으로 정규화했다.
- `first-class or upper second-class`처럼 범위를 함께 쓰는 경우 실제 최소 요구 등급인 upper second를 선택하도록 했다.
- 요구 등급과 정확히 매칭되지 않으면 한국 조건을 누락하지 않고 전문을 표시하며 수동 확인 상태로 남기도록 했다.
- 실제 QMUL DOM의 세 등급 후보를 읽고 upper second 조건이 `UK 2:1 degree` GPA 행으로 연결되는 것을 확인했다.
- 확장 버전을 `0.3.1`로 올리고 관련 자동 테스트를 추가했다.

## Decisions - 0.3.1 실사용 피드백 수정

- KCL 과정명은 URL 전체를 제목으로 추측하지 않고 metadata에서 이름을 얻은 뒤 URL은 MSc·MA 같은 학위 접미사 확인에만 사용한다.
- QMUL 한국 조건은 먼저 전문과 후보 행을 모두 보존하고, 공통 parser가 과정 기본 등급과 연결한다.
- 여러 허용 등급이 함께 적힌 문장은 가장 낮은 합격 가능 등급을 최소 요구 조건으로 본다.
- 등급 표현이 새롭거나 모호하면 잘못된 한 행을 보여주는 것보다 전문과 확인 필요 상태를 함께 보여준다.

## Current Status - 0.3.1 실사용 피드백 수정

- KCL 과정명 누락 수정 완료
- QMUL 2:1·upper second 매칭 수정 완료
- QMUL 매칭 실패 시 한국 조건 전문 표시 완료
- SOAS 변경 없음
- 자동 테스트 33개 통과
- 실제 설치 확장 `0.3.1` 재확인 대기

## Next Steps - 0.3.1 실사용 피드백 수정

- `chrome://extensions`에서 확장을 새로고침하고 버전 `0.3.1`을 확인한다.
- KCL에서 Course가 `Nutrition MSc`로 표시되는지 확인한다.
- J님이 확인한 QMUL 2:1 과정에서 Korean Academic Requirements가 `UK 2:1 degree` 행으로 표시되는지 확인한다.
- 새로운 표현으로 매칭되지 않는 과정은 한국 조건 전문과 확인 필요 안내가 모두 보이는지 확인한다.
